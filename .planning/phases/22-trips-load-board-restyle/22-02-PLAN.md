---
phase: 22-trips-load-board-restyle
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Truck tabs render inside a scrollable segmented control with white card lift on active tab -- no blue #1e40af fill, no badge trip counts"
    - "Status filter tabs (Active/Completed/All) render as a segmented control with white card lift active state and count in parentheses"
    - "Density toggle renders as a segmented control matching truck tabs styling, positioned in the header controls row"
    - "Trips desktop table uses .data-table class inside a .card.card-flush wrapper with no hardcoded #f8fafc or #e2e8f0 colors"
    - "Trip mobile cards use .trip-card CSS class with flat styling instead of inline styles"
    - "All density settings (compact/default/comfortable) work correctly with clean styling"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderTrips function with segmented controls, data-table, and trip-card classes"
  key_links:
    - from: "Truck tabs"
      to: "base.css .segmented-control-scroll"
      via: "CSS class"
      pattern: "segmented-control-scroll"
    - from: "Status filter and density toggle"
      to: "base.css .segmented-control"
      via: "CSS class"
      pattern: "segmented-control"
    - from: "Desktop table"
      to: "base.css .data-table"
      via: "CSS class"
      pattern: "data-table"
    - from: "Mobile cards"
      to: "base.css .trip-card"
      via: "CSS class"
      pattern: "trip-card"
---

<objective>
Restyle the entire renderTrips function -- truck tabs, status filter, density toggle, page header, desktop table, and mobile cards -- to use the Stripe/Linear aesthetic with segmented controls and flat styling.

Purpose: The trips page is the second most-used page. This plan replaces all hardcoded inline styles with CSS component classes established in Plan 01 and prior phases.

Output: A fully restyled trips page matching the aesthetic of the orders page (Phase 21).
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trips-load-board-restyle/22-CONTEXT.md
@.planning/phases/22-trips-load-board-restyle/22-RESEARCH.md
@.planning/phases/22-trips-load-board-restyle/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle truck tabs, status filter, and density toggle with segmented controls</name>
  <files>index.html</files>
  <action>
Find `renderTrips(c)` at line ~19167. Work through the function:

**Truck tabs (line ~19201-19209):**
1. Replace the entire truck tab button construction. Remove all inline style attributes (padding, border, cursor, font-weight, border-radius, margin, transition, background, color).
2. Each truck tab button should use: `class="segmented-control-btn${isActive ? ' active' : ''}"` with NO inline styles.
3. Show truck number only -- remove the trip count badge span entirely (per user decision: "Show truck number only -- no trip counts or other metadata on tabs").
4. The button text should be just the truck icon + truck number: `[[icon:truck]] ${t.truck_number}`.
5. Replace the wrapping `<div style="display:flex;overflow-x:auto;gap:4px;margin-bottom:16px;padding-bottom:4px;scrollbar-width:none;-webkit-overflow-scrolling:touch">` with `<div class="segmented-control-scroll" style="margin-bottom:var(--space-4)">`.
6. Remove the inactiveBg, inactiveColor, inactiveBorder, badgeBg variables -- they are no longer used.

**Status filter tabs (line ~19260-19265):**
1. Replace the `statusTabStyle` function and the entire status tabs construction.
2. Wrap status buttons in: `<div class="segmented-control" style="margin-bottom:var(--space-4)">`.
3. Each status button uses: `class="segmented-control-btn${isActive ? ' active' : ''}"` with NO inline styles.
4. Show count in parentheses after the label text, not as a separate badge span: e.g., `Active (${activeCount})`.
5. Remove the badge span with its inline background/padding/border-radius styles.

**Density toggle (line ~19299, 19312-19316):**
1. Replace the `densityBtnStyle` function and the inline density toggle construction.
2. Wrap density buttons in: `<div class="segmented-control">`.
3. Each density button uses: `class="segmented-control-btn${tripsDensity===d ? ' active' : ''}"` with NO inline styles.
4. Keep labels: "Compact", "Default", "Comfy".
5. Remove the wrapping div that had `border:1px solid var(--border);border-radius:6px;overflow:hidden` -- the segmented-control class handles this.

**Column visibility dropdown (line ~19306-19319):**
1. The trigger button -- change to `class="btn btn-secondary btn-sm"`. Remove inline styles (padding, border, border-radius, background, color, cursor, font-size, font-weight, display, align-items, gap).
2. Keep the dropdown menu (#trips-col-menu) `<style>` block as-is since it uses CSS variables already.

**Year selector (line ~19311):**
1. The year select dropdown -- add `class="select"`. Remove inline padding/border/border-radius/font-size styles.
2. The wrapping container -- simplify to `display:flex;align-items:center;gap:var(--space-2)`. Remove the background/padding/border-radius/border inline styles.
3. The "Year:" label -- use `class="text-label"` or apply `font-weight:500;color:var(--text-secondary);font-size:13px` inline.

**Page header (line ~19310):**
1. The h2 heading -- ensure font-size is 18px, font-weight 600.
2. Remove the [[icon:truck]] emoji from the heading text -- use plain text "Trips by Truck".
3. The "+ New Trip" button should remain `class="btn btn-primary"`.

**Inline <style> block (line ~19302-19308):**
1. Remove the density CSS rules from the inline `<style>` block -- they now live in base.css (Plan 01).
2. Keep the column-hide CSS (`hiddenColCSS`) and the #trips-col-menu styles in the inline `<style>` block -- those are dynamic and page-specific.
  </action>
  <verify>
Search renderTrips for:
- "#1e40af" -- should have zero matches (old blue active color removed)
- "#e2e8f0" in truck tabs area -- should be gone
- "#f1f5f9" in status tabs area -- should be gone
- "segmented-control" -- should find truck tabs, status filter, and density toggle using the class
- "badgeBg" or "inactiveBg" -- should be gone
- tripCount badge span in truck tabs -- should be gone
- densityBtnStyle function -- should be gone
- statusTabStyle function -- should be gone
  </verify>
  <done>Truck tabs, status filter, and density toggle all use segmented control pattern. Year selector uses .select class. Column visibility uses btn-secondary. No hardcoded blue/gray colors remain in controls.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle trips desktop table and mobile cards</name>
  <files>index.html</files>
  <action>
Still in `renderTrips(c)`, continuing from Task 1:

**Desktop table wrapper (line ~19326):**
1. Replace the desktop table wrapper. Currently: `<div class="desktop-only card density-${tripsDensity}">`. Change to: `<div class="desktop-only card card-flush density-${tripsDensity}">`.
2. The truck header banner inside the card: `<div style="padding:16px;background:#f8fafc;border-bottom:1px solid #e2e8f0">`. Replace with: `<div style="padding:var(--space-3) var(--space-4);border-bottom:1px solid var(--border)">`. Remove `background:#f8fafc` -- let it inherit card background for a cleaner look.
3. The truck number heading: `<strong style="font-size:18px">TRUCK ${...}</strong>`. Change to: `<strong style="font-size:15px;font-weight:600">Truck ${...}</strong>`. Smaller, not all-caps, weight 600.
4. The trip count/home text: Keep but use `style="color:var(--text-secondary);margin-left:var(--space-3);font-size:13px"`.

**Table element (line ~19326):**
5. Add `class="data-table"` to the `<table id="trips-table">` element: `<table id="trips-table" class="data-table">`.
6. The table already uses `<thead>` and `<tbody>` -- good. No changes needed to header cell text.

**Desktop table rows (line ~19226):**
7. The trip rows are built in the `tripRows` variable. They already use `getBadge()` for status badges and action-btn classes for buttons. Check for any inline colors:
   - The complete button has `style="background:#10b981;color:white"` -- replace with `class="action-btn" style="background:var(--green-dim);color:var(--green)"` for consistency with the desaturated button pattern, OR keep it as a solid green action since it's a primary action. Decision: keep the green complete button but use token: `style="background:var(--green);color:white"`.
   - Revenue cell: add `style="font-family:var(--font-mono)"` to the money td or the content span.

**Mobile cards (line ~19232-19250):**
8. Replace the mobile card inline styles with the `.trip-card` class. Change:
   ```
   <div style="background:var(--bg-card);border:1px solid var(--border);border-radius:var(--radius);padding:14px;margin-bottom:8px;cursor:pointer"
   ```
   to:
   ```
   <div class="trip-card" style="margin-bottom:var(--space-2)"
   ```
9. The header row inside mobile card: Replace `style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px"` with `class="trip-card-header"`.
10. Trip number span: Replace `style="font-weight:700;color:var(--primary);font-size:15px"` with `class="trip-number"`. This picks up 600 weight and text-primary color from the CSS class.
11. The metadata grid: Replace `style="display:grid;grid-template-columns:1fr 1fr;gap:4px;font-size:13px;color:var(--text-secondary);margin-bottom:10px"` with `class="trip-card-meta"`.
12. Inside metadata: Replace `style="color:var(--text-primary);font-weight:500"` spans with `<strong>` tags (the .trip-card-meta strong rule handles styling).
13. Revenue in mobile card: Replace `style="text-align:right;font-weight:600;color:var(--success,#10b981)"` with `style="text-align:right;font-weight:500;font-family:var(--font-mono);color:var(--text-primary)"` -- mono font, neutral color, 500 weight.
14. Action buttons row: Replace `style="display:flex;gap:8px"` with `class="trip-card-actions"`.
15. Mobile action buttons: Keep `min-height:44px` for touch targets. Remove the `style="min-height:44px;padding:10px 14px;flex:1"` from each and use `style="min-height:44px;flex:1"` -- let the btn class handle padding.
16. Mobile complete button: Use `style="background:var(--green);color:white;min-height:44px;flex:0 0 auto"` to match desktop.

**Mobile section header (line ~19331):**
17. Replace `style="padding:10px 0 12px;font-weight:700;font-size:15px;color:var(--text-secondary)"` with `style="padding:var(--space-2-5) 0 var(--space-3);font-weight:600;font-size:14px;color:var(--text-secondary)"` -- weight 600, 14px.

**Empty state (line ~19327, 19332):**
18. Desktop empty state already uses `renderEmptyState()` -- leave as-is.
19. Mobile empty state: Replace `style="text-align:center;padding:40px 16px;color:var(--text-muted)"` with a call to `renderEmptyState('truck', 'No trips yet', 'Tap + to create your first trip', '+ New Trip', 'openTripModal()')` if feasible, or at minimum use CSS tokens: `style="text-align:center;padding:var(--space-10) var(--space-4);color:var(--text-muted);font-size:13px"`.
  </action>
  <verify>
Search renderTrips for:
- "#f8fafc" -- should be gone (truck header background)
- "#e2e8f0" -- should be gone from table area
- "#10b981" -- should be replaced with var(--green) token
- "font-weight:700" -- should be gone (replaced with 600 or 500)
- "trip-card" class -- should appear in mobile card construction
- "data-table" class -- should appear on the trips table element
- "card-flush" -- should appear on the desktop table wrapper
  </verify>
  <done>Desktop table uses .data-table with .card-flush wrapper, mobile cards use .trip-card CSS classes, all hardcoded colors replaced with CSS tokens, density works across all three settings</done>
</task>

</tasks>

<verification>
1. Navigate to Trips page -- truck tabs display as a clean segmented control with white card lift on active
2. No blue #1e40af fill anywhere on the page
3. Status filter (Active/Completed/All) renders as segmented control with counts in parentheses
4. Density toggle renders as segmented control matching truck tabs style
5. Desktop table has clean hairline borders from .data-table class
6. Toggle between Compact/Default/Comfy -- padding adjusts correctly
7. Check mobile viewport (375px) -- trip cards display with flat .trip-card styling, touch targets are 44px
8. Click truck tabs -- correct truck's trips display (behavior unchanged)
9. Click status tabs -- correct filtering applies (behavior unchanged)
10. Column visibility dropdown still works
11. Year selector still filters correctly
</verification>

<success_criteria>
- Truck tabs use segmented control with truck number only (no badge counts)
- Status filter uses segmented control with count in parentheses
- Density toggle uses matching segmented control pattern
- Desktop table uses .data-table class with density overrides working
- Mobile cards use .trip-card class with flat styling
- No hardcoded hex colors (#1e40af, #e2e8f0, #f8fafc, #f1f5f9) remain in renderTrips
- All click handlers and behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/22-trips-load-board-restyle/22-02-SUMMARY.md`
</output>
