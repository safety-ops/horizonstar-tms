---
phase: 22-trips-load-board-restyle
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Load board category tabs render as a segmented control with white card lift active state -- no per-category colors on tab buttons"
    - "Load board subcategory tabs render as a segmented control with white card lift active state -- no blue #1e40af fill"
    - "Load board stats use .stat-flat class instead of .stat-card"
    - "Load board section header uses neutral styling -- no colored background with white text"
    - "Category count shows in parentheses on each tab button"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderLoadBoard function with segmented controls, flat stats, and neutral section headers"
  key_links:
    - from: "Category tabs"
      to: "base.css .segmented-control"
      via: "CSS class"
      pattern: "segmented-control"
    - from: "Stats"
      to: "base.css .stat-flat"
      via: "CSS class"
      pattern: "stat-flat"
    - from: "Order cards"
      to: "renderOrderPreviewCard (already restyled in Phase 21)"
      via: "function call"
      pattern: "renderOrderPreviewCard"
---

<objective>
Restyle the renderLoadBoard page function -- category tabs, subcategory tabs, stats row, section header, empty state, and page header -- to match the Stripe/Linear aesthetic.

Purpose: The load board is the staging area for unassigned orders. The order cards themselves are already restyled (renderOrderPreviewCard from Phase 21), so this plan only handles the surrounding chrome.

Output: A fully restyled load board page with segmented control tabs, flat stats, and neutral section headers.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trips-load-board-restyle/22-CONTEXT.md
@.planning/phases/22-trips-load-board-restyle/22-RESEARCH.md
@.planning/phases/22-trips-load-board-restyle/22-01-SUMMARY.md
@.planning/phases/22-trips-load-board-restyle/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle load board page header, stats, and category tabs</name>
  <files>index.html</files>
  <action>
Find `renderLoadBoard(c)` at line ~17650. Work through the function:

**Page header (line ~17676):**
1. The h2 heading "[[icon:clipboard]] Future Cars" -- ensure font-size 18px, font-weight 600. Remove the clipboard icon, use plain text "Future Cars".
2. The "+ Add Vehicle" button -- keep as `class="btn btn-primary"`.
3. The "AI Import" button -- change from purple `style="background:#8b5cf6;color:white"` to `class="btn btn-secondary"`. Remove the inline background/color styles. This matches the decision from Phase 21: only the primary action gets btn-primary.

**Stats row (line ~17679-17683):**
4. Replace the three `.stat-card` divs with `.stat-flat` class. Change `class="stat-card"` to `class="stat-flat"`.
5. Remove inline styles on stat divs: `style="flex:0 0 auto;min-width:120px;padding:12px 16px"`. The stat-flat class handles padding. Keep only `style="flex:0 0 auto;min-width:120px"` for layout.
6. Change the `.label` and `.value` divs inside stats to use `.stat-card-label` and `.stat-card-value` classes (established in Phase 20). If the current HTML uses `<div class="label">` and `<div class="value">`, update to `<div class="stat-card-label">` and `<div class="stat-card-value">`.
7. Revenue stat value should use `font-family:var(--font-mono)` for the dollar amount.

**Category tabs (line ~17686-17691):**
8. Replace the wrapping div: change `<div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px">` to `<div class="segmented-control" style="margin-bottom:var(--space-4);flex-wrap:wrap">`.
9. Each category button: replace the entire inline style construction. Remove the `style="padding:10px 16px;border:2px solid ${cat.color};background:${isActive ? cat.color : inactiveBg};color:${isActive ? 'white' : cat.color};border-radius:8px;cursor:pointer;font-weight:600"`.
10. Replace with: `class="segmented-control-btn${isActive ? ' active' : ''}"` and NO inline styles.
11. Button text should show category name with count in parentheses: `${cat.name} (${categoryCounts[cat.id]})`.
12. Remove the `inactiveBg` variable reference -- no longer needed.
13. The `loadBoardCategories` data array (line ~17627) keeps its `color` property unchanged -- we just stop using `cat.color` in the tab button styles.

**Subcategory tabs (line ~17695-17701):**
14. Replace the wrapping div: change to `<div class="segmented-control" style="margin-bottom:var(--space-4);margin-left:var(--space-5)">`.
15. The "All" button and each subcategory button: replace inline styles with `class="segmented-control-btn${isActive ? ' active' : ''}"`.
16. Remove all references to `#cbd5e1`, `#1e40af`, and hardcoded white/gray colors.
17. Subcategory buttons show name with count: `${sub.name} (${categoryCounts[sub.id]})`. The "All" button shows just "All".

**Section header (line ~17704):**
18. Replace the colored card header: `<div class="card-header" style="background:${currentCat.color};color:white;border-radius:8px 8px 0 0"><h3 style="margin:0">`.
19. Change to a flat section header: `<div style="padding:var(--space-3) var(--space-4);border-bottom:1px solid var(--border)"><h3 style="margin:0;font-size:15px;font-weight:600;color:var(--text-primary)">`.
20. Remove the `border-radius:8px 8px 0 0` -- the parent card handles border-radius.
21. Keep the category name and subcategory arrow text content unchanged.

**Empty state (line ~17705):**
22. Replace `<div style="padding:40px;text-align:center;color:#64748b">No vehicles in this category</div>` with a call to `renderEmptyState('clipboard', 'No vehicles', 'No unassigned vehicles in this category', '+ Add Vehicle', 'openLoadBoardOrder()')`.

**Orders container (line ~17706):**
23. The cards container `<div style="padding:12px;display:flex;flex-direction:column;gap:10px">` -- replace with `<div style="padding:var(--space-3);display:flex;flex-direction:column;gap:var(--space-2-5)">`.

**Wrapping card (line ~17704):**
24. The outer `<div class="card">` wrapping the section header + orders is fine. Ensure it uses the card class (it already does).

**Order cards (line ~17707-17724):**
25. The order cards already use `renderOrderPreviewCard()` which was restyled in Phase 21. No changes needed to the card rendering itself.
26. However, check the `actionsHtml` and `extraMetaHtml` passed to renderOrderPreviewCard:
    - Action buttons have `style="padding:5px 9px;font-size:12px"` -- keep these, they're appropriate sizing.
    - The extraBottomHtml has inline dim-blue badge: keep as-is, it uses CSS variable tokens already.
    - The broker fee/local fee labels use `color:var(--danger)` for the amount -- keep this functional color coding.
  </action>
  <verify>
Search renderLoadBoard for:
- "#8b5cf6" (purple) -- should be gone (AI Import button)
- "#1e40af" (blue) -- should be gone (subcategory active state)
- "#cbd5e1" -- should be gone (subcategory border)
- "cat.color" in button styles -- should NOT appear in style attributes (only kept in the data array)
- "#64748b" -- should be gone (empty state text color)
- "segmented-control" -- should appear for category and subcategory tabs
- "stat-flat" -- should appear for stats
- "btn btn-secondary" -- should appear for AI Import button
Open the app, navigate to Future Cars:
- Category tabs are neutral segmented control (no per-category colors)
- Stats are flat style
- Section header is neutral (no colored background)
- Order cards render correctly (inherited from Phase 21)
  </verify>
  <done>Load board page uses segmented controls for category/subcategory tabs, flat stats, neutral section header, and consistent button hierarchy. All per-category color coding removed from tab buttons. Order cards remain restyled from Phase 21.</done>
</task>

</tasks>

<verification>
1. Navigate to Future Cars / Load Board page -- category tabs are neutral segmented control
2. No per-category colors on any tab button (blue, purple, cyan, green, amber, red, pink all gone from buttons)
3. Click between categories -- active state shows white card lift, not colored fill
4. Subcategory tabs match the same segmented control pattern
5. Stats show flat style (no elevated card)
6. Section header is neutral (no colored background band)
7. AI Import button is secondary (not purple)
8. Empty category shows proper empty state with icon
9. Order cards still render correctly with Phase 21 styling
10. Mobile viewport -- tabs wrap cleanly, cards stack vertically
11. All click handlers work -- category switching, subcategory filtering, assign, edit, delete
</verification>

<success_criteria>
- Category tabs use segmented control with counts in parentheses, no per-category colors
- Subcategory tabs use matching segmented control pattern
- Stats use .stat-flat class
- Section header is neutral (no colored background)
- AI Import button is btn-secondary (not purple)
- Empty state uses renderEmptyState helper
- No hardcoded hex colors remain in renderLoadBoard
- All click handlers and behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/22-trips-load-board-restyle/22-03-SUMMARY.md`
</output>
