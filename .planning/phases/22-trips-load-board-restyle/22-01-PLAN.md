---
phase: 22-trips-load-board-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/base.css
autonomous: true

must_haves:
  truths:
    - "A .segmented-control component exists in base.css with contained pill-style buttons that lift active state with white card background and subtle shadow"
    - "A .trip-card component exists in base.css with flat card styling, hairline border, and subtle hover elevation"
    - "Density override rules exist in base.css that adjust .data-table padding for compact/default/comfortable modes"
  artifacts:
    - path: "assets/css/base.css"
      provides: "Segmented control component (.segmented-control, .segmented-control-btn), trip card component (.trip-card), and density override rules"
      contains: "segmented-control"
  key_links:
    - from: ".segmented-control"
      to: "Truck tabs, status filter, density toggle, load board category tabs"
      via: "CSS class applied in Plans 02 and 03"
      pattern: "segmented-control"
    - from: ".trip-card"
      to: "Trip mobile cards and potential desktop card view"
      via: "CSS class applied in Plan 02"
      pattern: "trip-card"
---

<objective>
Add three new CSS components to base.css: segmented control (for truck tabs, status filter, density toggle, load board category tabs), trip card (for trip card view), and density override rules (for trips table compact/default/comfortable modes).

Purpose: These reusable components are needed by both the Trips page (Plan 02) and Load Board page (Plan 03). Building them first in base.css lets both plans reference clean class names instead of inline styles.

Output: Three new component blocks in base.css ready for use.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trips-load-board-restyle/22-CONTEXT.md
@.planning/phases/22-trips-load-board-restyle/22-RESEARCH.md
@assets/css/base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add segmented control, trip card, and density components to base.css</name>
  <files>assets/css/base.css</files>
  <action>
Add three component blocks to base.css, placed after the existing button/badge component sections:

**1. Segmented Control Component:**
```css
/* Segmented Control */
.segmented-control {
  display: inline-flex;
  align-items: center;
  gap: 0;
  padding: 2px;
  background: var(--bg-tertiary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
}
.segmented-control-btn {
  padding: 6px 14px;
  font-size: 13px;
  font-weight: 500;
  color: var(--text-secondary);
  background: transparent;
  border: none;
  border-radius: calc(var(--radius-sm) - 2px);
  cursor: pointer;
  transition: background 150ms ease, color 150ms ease, box-shadow 150ms ease;
  white-space: nowrap;
  font-family: inherit;
  line-height: 1.4;
}
.segmented-control-btn.active {
  background: var(--bg-card);
  color: var(--text-primary);
  box-shadow: var(--shadow-xs);
  font-weight: 600;
}
.segmented-control-btn:hover:not(.active) {
  color: var(--text-primary);
}
```

Add a scrollable variant for truck tabs (many items):
```css
/* Segmented control — scrollable variant for many items */
.segmented-control-scroll {
  display: flex;
  align-items: center;
  gap: 0;
  padding: 2px;
  background: var(--bg-tertiary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  overflow-x: auto;
  scrollbar-width: none;
  -webkit-overflow-scrolling: touch;
}
.segmented-control-scroll::-webkit-scrollbar { display: none; }
```

**2. Trip Card Component:**
```css
/* Trip Card */
.trip-card {
  border: 1px solid var(--border);
  background: var(--bg-card);
  border-radius: var(--radius);
  padding: var(--space-3) var(--space-4);
  cursor: pointer;
  transition: border-color 150ms ease, box-shadow 150ms ease;
}
.trip-card:hover {
  border-color: var(--border-strong, var(--text-muted));
  box-shadow: var(--shadow-sm);
}
.trip-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-2);
}
.trip-card-header .trip-number {
  font-weight: 600;
  font-size: 14px;
  color: var(--text-primary);
}
.trip-card-meta {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-1);
  font-size: 13px;
  color: var(--text-secondary);
  margin-bottom: var(--space-2-5);
}
.trip-card-meta strong {
  color: var(--text-primary);
  font-weight: 500;
}
.trip-card-actions {
  display: flex;
  gap: var(--space-2);
}
```

**3. Density Override Rules:**
```css
/* Density overrides for data-table */
.density-compact .data-table td,
.density-compact .data-table th { padding: 6px 12px; }
.density-default .data-table td,
.density-default .data-table th { padding: 10px 14px; }
.density-comfortable .data-table td,
.density-comfortable .data-table th { padding: 14px 20px; }
```

IMPORTANT: Do NOT use --border-primary (not defined in variables.css). Use --border. Do NOT use --radius-md (not defined). Use --radius and --radius-sm only.
  </action>
  <verify>
Open base.css and confirm:
- .segmented-control and .segmented-control-btn classes exist with active state using bg-card + shadow-xs
- .segmented-control-scroll variant exists with overflow-x:auto
- .trip-card class exists with border, bg-card, radius, hover elevation
- .density-compact/.density-default/.density-comfortable rules exist targeting .data-table
- No references to --border-primary or --radius-md (undefined tokens)
  </verify>
  <done>base.css contains segmented control component (inline + scrollable), trip card component with header/meta/actions sub-classes, and density override rules for data-table</done>
</task>

</tasks>

<verification>
1. Open base.css and search for "segmented-control" -- should find the component block
2. Search for "trip-card" -- should find the card component block
3. Search for "density-compact" -- should find the override rules
4. Verify no usage of --border-primary or --radius-md tokens (undefined)
5. Verify the segmented control active state uses bg-card + shadow-xs (white card lift pattern)
</verification>

<success_criteria>
- Segmented control component ready for use by truck tabs, status filter, density toggle, and load board tabs
- Trip card component ready for use by mobile/desktop trip cards
- Density rules ready to replace inline <style> density CSS in renderTrips
- All CSS uses only tokens defined in variables.css
</success_criteria>

<output>
After completion, create `.planning/phases/22-trips-load-board-restyle/22-01-SUMMARY.md`
</output>
