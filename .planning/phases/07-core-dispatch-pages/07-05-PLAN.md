---
phase: 07-core-dispatch-pages
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - mockups/web-tms-redesign/orders.html
autonomous: false

must_haves:
  truths:
    - "Orders page shows horizontal filter bar with search input and 4 dropdowns (Status, Dispatcher, Driver, Broker)"
    - "Order table displays rows with checkboxes for bulk selection"
    - "Table has inline View/Edit/Del action buttons in sticky right column"
    - "Bulk action buttons appear above the table (Assign to Trip, Export, Delete Selected)"
    - "Status badges use correct colors (PENDING=amber, IN_TRANSIT=blue, DELIVERED=green)"
    - "Showing X of Y count indicator is present"
    - "Light/dark theme toggle works and all elements render correctly in both themes"
  artifacts:
    - path: "mockups/web-tms-redesign/orders.html"
      provides: "Complete Orders list mockup with filters, checkboxes, and order table"
      min_lines: 250
  key_links:
    - from: "orders.html"
      to: "shared.css"
      via: "link rel stylesheet"
      pattern: 'href="./shared.css"'
    - from: "orders.html"
      to: "base-template.html"
      via: "copied app shell (sidebar + header + page-content)"
      pattern: "class=\"app-layout\""
---

<objective>
Create the Orders list mockup page with horizontal filter bar, bulk selection checkboxes, order data table, and inline action buttons.

Purpose: The Orders page is where dispatchers manage all vehicle orders — searching, filtering, selecting for bulk operations, and drilling into individual order details. This mockup reproduces the exact filter bar, table structure, and action pattern from production.

Output: `mockups/web-tms-redesign/orders.html` — a complete, standalone HTML file with filter bar, order table with checkboxes, bulk actions, and inline row actions.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-dispatch-pages/07-CONTEXT.md
@.planning/phases/07-core-dispatch-pages/07-RESEARCH.md
@mockups/web-tms-redesign/shared.css
@mockups/web-tms-redesign/base-template.html
@mockups/web-tms-redesign/component-showcase.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create orders.html with filter bar, bulk actions, and order table</name>
  <files>mockups/web-tms-redesign/orders.html</files>
  <action>
Create `orders.html` by copying the app shell from `base-template.html`. Set page title to "Horizon Star TMS — Orders", header title to "Orders", and mark the Load Board nav item as `active` (Orders is accessed via Load Board in production nav, but if there's a dedicated Orders nav item use that; otherwise keep Load Board active).

Actually, looking at base-template.html, there is no dedicated Orders nav item. In production, Orders is a sub-view. For the mockup, keep the Load Board nav item as active since orders are managed from there. Alternatively, do NOT mark any nav item as active for this page (remove the `active` class from Dashboard).

Inside `<div class="page-content">`, build these sections:

**Section 1: Page Header Row**
Flex row:
- Left: "Orders" as a page subtitle (the header bar already has "Orders" as title)
- Right: "New Order" primary button with plus icon + "Import Orders" secondary button

**Section 2: Horizontal Filter Bar**
A card-style background row (`.card` or styled div with `background: var(--bg-card); padding: 16px; border-radius: 12px;`) containing:

- **Search input** (flex: 1, min-width: 200px): `<input class="form-input" placeholder="Search by Order ID, Broker, or Vehicle...">`
- **Status dropdown**: Label "Status:" + `<select class="form-input">` with options: All, Unassigned, Assigned, PENDING, IN_TRANSIT, DELIVERED
- **Dispatcher dropdown**: Label "Dispatcher:" + select with: All, Maria Garcia, David Chen, Sarah Johnson
- **Driver dropdown**: Label "Driver:" + select with: All, John Smith, Maria Garcia, Robert Williams, David Chen
- **Broker dropdown**: Label "Broker:" + select with: All Brokers, Central Dispatch, AutoShip Pro, RoadMaster, Express Auto, National Carrier
- **Clear button**: `.btn.btn-secondary` "Clear"
- **Count indicator**: `<span>` with "Showing 47 of 150" in text-muted, text-xs

Use `display: flex; gap: 16px; flex-wrap: wrap; align-items: center;` for the filter row.
Each dropdown group: `display: flex; align-items: center; gap: 8px;` with label in `font-weight: 600; color: var(--text-secondary); font-size: var(--text-xs); text-transform: uppercase; white-space: nowrap;`

**Section 3: Bulk Action Bar**
A flex row between filter bar and table showing:
- Left: Checkbox "Select All" + "3 selected" count text
- Right: "Assign to Trip" primary button (disabled style) + "Export CSV" secondary button + "Delete Selected" danger button (disabled style)

Disabled buttons: `opacity: 0.5; cursor: not-allowed;`
Show "3 selected" to indicate some checkboxes are ticked in the table below.

**Section 4: Order Table**
Card with `.table-container` wrapping the table.

Table columns (matching production `renderOrders()`):
Checkbox | Order # | Broker | Vehicle | Year/Make/Model | Origin | Destination | Pickup Date | Delivery Date | Status | Pay Type | Price | Trip | Dispatcher | Actions (sticky-col)

8 rows of realistic order data. Mark 3 rows as "checked" (checkbox checked attribute) to match the "3 selected" indicator:

1. [x] ORD-2024-001 / Central Dispatch / 2024 Toyota Camry / Black / Miami, FL / New York, NY / 2025-01-20 / 2025-01-22 / DELIVERED (badge-green) / COD (badge-green) / $1,150.00 / TRIP 3-77-2025 / Maria Garcia / View Edit Del
2. [x] ORD-2024-015 / AutoShip Pro / 2023 Honda Civic / Silver / Orlando, FL / Brooklyn, NY / 2025-01-20 / 2025-01-23 / IN_TRANSIT (badge-blue) / COP (badge-blue) / $950.00 / TRIP 3-77-2025 / Maria Garcia / View Edit Del
3. [ ] ORD-2024-027 / RoadMaster / 2025 Ford Mustang / Red / Tampa, FL / Queens, NY / 2025-01-21 / 2025-01-24 / PENDING (badge-amber) / BILL (badge-red) / $1,350.00 / — / David Chen / View Edit Del
4. [x] ORD-2024-033 / Express Auto / 2022 Tesla Model 3 / White / Jacksonville, FL / Bronx, NY / 2025-01-21 / 2025-01-23 / DELIVERED (badge-green) / COD (badge-green) / $1,275.00 / TRIP 3-77-2025 / Maria Garcia / View Edit Del
5. [ ] ORD-2025-003 / Central Dispatch / 2024 BMW X5 / Blue / Atlanta, GA / Manhattan, NY / 2025-01-22 / 2025-01-25 / PENDING (badge-amber) / COD (badge-green) / $1,450.00 / — / Sarah Johnson / View Edit Del
6. [ ] ORD-2025-008 / National Carrier / 2023 Chevrolet Malibu / Gray / Savannah, GA / Staten Island, NY / 2025-01-22 / 2025-01-24 / IN_TRANSIT (badge-blue) / COP (badge-blue) / $875.00 / TRIP 5-88-2025 / David Chen / View Edit Del
7. [ ] ORD-2025-011 / AutoShip Pro / 2024 Hyundai Tucson / White / Charlotte, NC / Jersey City, NJ / 2025-01-23 / 2025-01-26 / PENDING (badge-amber) / CHECK (badge-purple or secondary) / $1,025.00 / — / Sarah Johnson / View Edit Del
8. [ ] ORD-2025-014 / RoadMaster / 2025 Toyota RAV4 / Silver / Nashville, TN / Newark, NJ / 2025-01-23 / 2025-01-26 / DELIVERED (badge-green) / BILL (badge-red) / $1,175.00 / TRIP 7-77-2025 / Maria Garcia / View Edit Del

Column formatting:
- Order #: `.font-mono`
- Price: `.mono.text-right`
- Trip: `.font-mono`, show "—" if unassigned
- Checkbox: `<input type="checkbox">` with some `checked`
- Status + Pay Type: use badge classes
- Actions: `.sticky-col` with view (eye), edit (edit), delete (trash) icon buttons

**Section 5: Pagination Row**
Below the table, a flex row:
- Left: "Showing 1-8 of 47 orders" text (text-muted, text-sm)
- Right: Pagination buttons: "Prev" (disabled) + page numbers (1 active, 2, 3, 4, 5, "...") + "Next"
- Use `.btn.btn-secondary` for page buttons, `.btn.btn-primary` for active page

**Spacing:** 16px between header row and filter bar, 12px between filter bar and bulk actions, 0px between bulk actions and table (they're visually connected), 16px between table and pagination.
  </action>
  <verify>
Open `mockups/web-tms-redesign/orders.html` in a browser. Verify:
1. Page loads without errors
2. Filter bar shows search input + 4 dropdowns + Clear button + count
3. Bulk action bar shows Select All checkbox and action buttons
4. Order table shows 8 rows with checkboxes (3 checked)
5. Status and pay type badges use correct colors
6. Price column is right-aligned monospaced
7. Action buttons (View/Edit/Del) in sticky right column
8. Pagination row shows page numbers
9. Theme toggle works
10. Sidebar collapses/expands
  </verify>
  <done>orders.html exists with horizontal filter bar (search + 4 dropdowns), bulk action bar, 8-row order table with checkboxes, and pagination, using shared.css classes exclusively</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Orders list mockup with horizontal filter bar (search + Status/Dispatcher/Driver/Broker dropdowns), bulk action bar (Select All, Assign to Trip, Export, Delete), 8-row order table with selection checkboxes and inline View/Edit/Del buttons, and pagination.</what-built>
  <how-to-verify>
1. Open `mockups/web-tms-redesign/orders.html` in your browser
2. Verify filter bar: search input, 4 dropdowns (Status, Dispatcher, Driver, Broker), Clear button, "Showing 47 of 150"
3. Verify bulk action bar: Select All checkbox, 3 selected count, Assign/Export/Delete buttons
4. Check order table: 8 rows, 3 with checked checkboxes, columns for Order #, Broker, Vehicle, Origin, Dest, Dates, Status, Pay Type, Price, Trip, Dispatcher, Actions
5. Verify status badges (PENDING=amber, IN_TRANSIT=blue, DELIVERED=green)
6. Verify pay type badges (COD=green, COP=blue, BILL=red, CHECK=purple)
7. Check pagination row below table
8. Toggle dark/light theme — filter bar and table should look clean in both
9. Confirm layout matches your current Orders page structure
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] orders.html loads in browser without console errors
- [ ] Horizontal filter bar with search + 4 dropdowns
- [ ] Bulk action bar with checkbox selection UI
- [ ] Order table has 8 rows with checkboxes
- [ ] 3 rows shown as checked, matching "3 selected" count
- [ ] Status and pay type badges use correct colors
- [ ] Price column monospaced and right-aligned
- [ ] Action buttons in sticky right column
- [ ] Pagination row present
- [ ] shared.css is the only stylesheet
- [ ] Theme toggle works
</verification>

<success_criteria>
Orders mockup faithfully reproduces the production Orders page with filter bar, bulk selection, order table, and pagination, reskinned with Phase 6 design system, with realistic data and working theme toggle.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-dispatch-pages/07-05-SUMMARY.md`
</output>
