---
phase: 21-orders-page-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/base.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "Order preview cards across all 8 pages render with flat backgrounds, hairline borders, compact spacing, and no gradients or glow"
    - "Pagination controls across all pages render as flat inline elements with hairline top border instead of card-style containers"
    - "Status badges from getBadge() render with desaturated tint backgrounds matching the base.css badge system"
  artifacts:
    - path: "assets/css/base.css"
      provides: "Badge CSS rules for getBadge() class names (badge-green, badge-amber, badge-blue, badge-red, badge-gray)"
      contains: "badge-green"
    - path: "index.html"
      provides: "Restyled renderOrderPreviewCard and renderPaginationControls shared helpers"
  key_links:
    - from: "getBadge() function"
      to: "base.css badge rules"
      via: "CSS class names badge-green/amber/blue/red/gray"
      pattern: "badge-(green|amber|blue|red|gray)"
    - from: "renderOrderPreviewCard"
      to: "8 call sites across codebase"
      via: "function call"
      pattern: "renderOrderPreviewCard"
---

<objective>
Restyle the two shared helpers (renderOrderPreviewCard and renderPaginationControls) plus add missing badge CSS rules so that all 8+ pages using these helpers automatically inherit the Stripe/Linear aesthetic.

Purpose: These shared helpers propagate to orders, loadboard, trips, local drivers, brokers, and dealer portal -- restyling them once fixes the majority of order card appearances across the entire app.

Output: Flat order preview cards with hairline borders, compact pagination controls, and working desaturated badge classes.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-orders-page-restyle/21-CONTEXT.md
@.planning/phases/21-orders-page-restyle/21-RESEARCH.md
@assets/css/base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing badge CSS rules and order card CSS classes to base.css</name>
  <files>assets/css/base.css</files>
  <action>
Add CSS rules to base.css for the badge class names that getBadge() (line ~10270 in index.html) returns. The function returns badge-green, badge-amber, badge-blue, badge-red, badge-gray but base.css only has badge-success, badge-warning, badge-info, badge-danger, badge-neutral. Add rules that map to the same color tokens:

```css
/* getBadge() compatibility — maps to same tokens as semantic badge names */
.badge-green { background: var(--green-dim); color: var(--green); }
.badge-amber { background: var(--amber-dim); color: var(--amber); }
.badge-blue { background: var(--blue-dim); color: var(--blue); }
.badge-red { background: var(--red-dim); color: var(--red); }
.badge-gray { background: var(--bg-tertiary); color: var(--text-secondary); }
```

Use the same font-size, padding, border-radius, and font-weight as the existing badge classes in base.css (should inherit from base `.badge-*` pattern or add matching declarations).

Also add a `.pagination-flat` class for the pagination control restyle:
```css
.pagination-flat {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-3);
  padding: var(--space-3) 0;
  border-top: 1px solid var(--border-primary);
  font-size: 13px;
  color: var(--text-secondary);
}
```
  </action>
  <verify>Open base.css and confirm badge-green/amber/blue/red/gray rules exist alongside existing badge-success/warning/info/danger/neutral rules. Confirm pagination-flat class exists.</verify>
  <done>base.css contains CSS rules for all 5 getBadge() class names plus pagination-flat utility class</done>
</task>

<task type="auto">
  <name>Task 2: Restyle renderOrderPreviewCard and renderPaginationControls in index.html</name>
  <files>index.html</files>
  <action>
**renderOrderPreviewCard (line ~10598-10712):**

1. Find the `cardStyle` variable built at line ~10668. Replace the inline style construction with cleaner values:
   - Remove `border-radius: 10px` -- use `border-radius: var(--radius-md)` (8px from tokens)
   - Remove `padding: 12px 16px` -- use `padding: var(--space-3) var(--space-4)`
   - Remove the `border: 1px solid ...` inline style -- use `border: 1px solid var(--border-primary)`
   - Remove `background: ...` inline -- use `background: var(--bg-primary)`
   - Keep `cursor:pointer` and `transition`
   - Remove the `options.accentColor` border-left:4px logic -- flat cards should NOT have colored left accent borders (per discretion: hairline border, no left accent by default)

2. Find the order number link at line ~10682 (`.order-card-link`). It already renders at 14px -- keep that. Ensure font-weight is 600 max (not 700/800).

3. Find any inline `font-weight: bold` or `font-weight: 700/800` in the function and replace with `font-weight: 600` or `font-weight: 500`.

4. Find the revenue/amount display. Ensure it uses `font-family: var(--font-mono)` for the dollar amount (scannable numbers).

5. Find the status badge output. It already uses getBadge() which returns badge-green/amber/blue/red/gray classes -- Task 1 added the CSS for these. Verify the badge `<span>` has the right class structure. If badges use inline background-color styles, remove them and rely on the CSS classes instead.

6. Keep the colored action buttons (dim-blue Assign, dim-amber Note, red Del) exactly as they are -- user decision to retain functional color coding.

7. Keep the note indicator amber treatment as-is.

8. DO NOT change class names or element order -- mobile CSS at lines 5737-5835 uses structural selectors that depend on the current DOM structure.

**renderPaginationControls (line ~10066-10079):**

1. Replace the card-style container. Remove inline styles for `background`, `border-radius: 12px`, `padding: 16px`.
2. Use the `pagination-flat` class from Task 1 instead (or apply inline: `border-top: 1px solid var(--border-primary); padding: var(--space-3) 0; display:flex; align-items:center; justify-content:center; gap: var(--space-3);`).
3. Keep the Previous/Next button structure. Style them as btn-secondary (add `class="btn btn-secondary btn-sm"` if not already classed, or keep inline styles but flatten: no background, just border, flat hover).
4. Keep the page count text centered between buttons. Use `color: var(--text-secondary); font-size: 13px;`.
  </action>
  <verify>
Search index.html for renderOrderPreviewCard and renderPaginationControls to confirm changes are applied:
- grep for "border-radius: 10px" near line 10668 -- should be gone
- grep for "accentColor" in renderOrderPreviewCard -- border-left logic should be removed
- grep for "border-radius: 12px" near renderPaginationControls -- should be gone
- Open the app in browser, navigate to Orders page, confirm cards render flat with hairline borders
  </verify>
  <done>renderOrderPreviewCard renders flat cards with hairline borders, no left accent, 600 max font-weight, mono dollar amounts. renderPaginationControls renders as flat inline element with hairline top border. All 8 pages using these helpers inherit the new look.</done>
</task>

</tasks>

<verification>
1. Open app in browser, navigate to Orders page -- cards should show flat backgrounds, hairline borders, no colored left accent, compact spacing
2. Navigate to Trips page, check order cards in trip detail -- same flat style inherited
3. Check pagination on any paginated page -- flat with hairline top border, no card container
4. Inspect a status badge -- should use desaturated tint background from CSS class (not inline color)
5. Check mobile viewport (375px) -- cards should still layout correctly (mobile CSS structural selectors intact)
</verification>

<success_criteria>
- Order preview cards across all 8 usage sites render with flat Stripe/Linear aesthetic
- Pagination controls render as flat elements instead of card containers
- getBadge() status badges display with desaturated tint backgrounds via CSS classes
- No mobile layout regressions
- No class name or DOM structure changes that break mobile CSS selectors
</success_criteria>

<output>
After completion, create `.planning/phases/21-orders-page-restyle/21-01-SUMMARY.md`
</output>
