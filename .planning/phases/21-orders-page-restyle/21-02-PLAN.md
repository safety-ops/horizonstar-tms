---
phase: 21-orders-page-restyle
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Orders page header uses compact typography with neutral button hierarchy (only New Order is btn-primary, AI Import and Export are btn-secondary)"
    - "Filter bar uses clean labels and inputs with consistent spacing -- no heavy inline styles"
    - "Table view uses .data-table class with hairline borders, refined headers, and subtle alternating row tint"
    - "Order detail modal follows the Stripe/Linear aesthetic with flat surfaces and consistent padding"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderOrders function (header, filters, card view, table view)"
  key_links:
    - from: "renderOrders"
      to: "renderOrderPreviewCard (Plan 01)"
      via: "function call in card view rendering"
      pattern: "renderOrderPreviewCard"
    - from: "renderOrders table view"
      to: "base.css .data-table"
      via: "CSS class"
      pattern: "data-table"
    - from: "filter bar selects"
      to: "base.css .select"
      via: "CSS class"
      pattern: "class=\"select"
---

<objective>
Restyle the renderOrders page function -- header, filter bar, view toggle, table view, year/undated chips, and button hierarchy -- to complete the orders page Stripe/Linear transformation.

Purpose: The orders page is the most-used page in the app. This plan handles all the page-level chrome around the order cards (which were restyled in Plan 01).

Output: A fully restyled orders page with clean header, flat filter bar, refined table view, and consistent button hierarchy.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-orders-page-restyle/21-CONTEXT.md
@.planning/phases/21-orders-page-restyle/21-RESEARCH.md
@.planning/phases/21-orders-page-restyle/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle orders page header, filter bar, and view toggle</name>
  <files>index.html</files>
  <action>
Find `renderOrders(c)` at line ~21703. Work through the function top to bottom:

**Page header (first ~20 lines of the function):**
1. The h2 "Vehicles / Orders" heading -- ensure font-size 18px, font-weight 600 (matching Phase 20 page header pattern).
2. Year selector dropdown -- add `class="select"` to use the base.css select component. Remove any inline padding/border/background styles that conflict.
3. Year/undated info chips -- replace `border-radius: 999px` pill styling with `badge-xs` pattern (4px radius, 10px font, 2px 6px padding). Use `badge-neutral` or `badge-gray` class if applicable, otherwise apply: `background: var(--bg-tertiary); color: var(--text-secondary); font-size: 10px; padding: 2px 6px; border-radius: 4px;`.
4. View toggle buttons (card/table) -- active state should use dark slate fill (`background: var(--btn-primary-bg); color: white;`), inactive should be transparent with `color: var(--text-secondary)`. Remove any heavy borders or colored active states.

**Action buttons:**
5. "+ New Order" button -- ensure it uses `btn btn-primary` (dark slate). Remove inline background-color if present.
6. "AI Import" button -- change from purple (#8b5cf6) to `btn btn-secondary`. User decision: only New Order gets primary treatment.
7. "Export" button -- ensure it uses `btn btn-secondary`.
8. Any other header buttons -- `btn btn-secondary` or `btn btn-ghost`.

**Filter bar:**
9. Search input -- add `class="input"` to use base.css input styling. Remove conflicting inline styles.
10. Filter select dropdowns (Status, Broker, etc.) -- add `class="select"` to each. Remove conflicting inline padding/border/background.
11. Clear filters button -- `btn btn-ghost btn-sm`.
12. Result count text -- `color: var(--text-secondary); font-size: 13px;`.
13. Remove any heavy inline styles on the filter container itself. Use `display: flex; flex-wrap: wrap; gap: var(--space-3); align-items: center; padding: var(--space-3) 0;`.

**View toggle styling:**
14. The card/table toggle should be a small segmented control. Apply: `display: inline-flex; gap: 0; border: 1px solid var(--border-primary); border-radius: var(--radius-md); overflow: hidden;` on the container, and each button gets `padding: var(--space-1-5) var(--space-3); font-size: 13px; border: none; cursor: pointer;`.
  </action>
  <verify>
Open the app, navigate to Orders:
- Header shows 18px heading, year selector uses .select class
- Only "+ New Order" is dark slate (btn-primary), AI Import and Export are secondary
- Filter bar has clean inputs with consistent gap spacing
- View toggle has dark slate active state
- Year/undated chips are small badge-xs style (4px radius)
  </verify>
  <done>Orders page header, filter bar, and view toggle follow Stripe/Linear aesthetic with correct button hierarchy and clean component classes</done>
</task>

<task type="auto">
  <name>Task 2: Restyle orders table view and clean up card view wrapper</name>
  <files>index.html</files>
  <action>
Still in `renderOrders(c)` (line ~21703):

**Table view (the `else` branch of the card/table view toggle):**
1. Find the `<table` element in the table view branch. Add `class="data-table"` to apply the base.css table styling (hairline borders, refined headers, subtle alternating row tint via nth-child). If it already has a class, append `data-table` to the class list.
2. Wrap the table in a `card-flush` container: `<div class="card card-flush" style="overflow-x:auto;">`. The card-flush class gives edge-to-edge content with no inner padding. Keep overflow-x:auto for horizontal scroll on narrow screens.
3. Table header cells (`<th>`) -- remove any inline background-color, font-weight, or padding styles. The .data-table th rule in base.css handles this (bg-tertiary background, 600 weight, 10px 14px padding, 13px font).
4. Table body cells (`<td>`) -- remove inline padding styles. .data-table td handles padding (10px 14px) and font-size (13px).
5. Remove any inline `border-collapse`, `border-spacing`, or `width:100%` on the table -- .data-table handles these.
6. Status badges in table rows -- they already use getBadge() which now has CSS classes from Plan 01. Remove any remaining inline background-color on badge spans.
7. Action buttons in table rows -- keep colored treatment (dim-blue Assign, red Del) per user decision. Ensure they use small sizing: `font-size: 12px; padding: 2px 8px;`.

**Card view wrapper:**
8. The card view branch wraps order cards in a `.card` container. Ensure it uses `padding: var(--space-4)` or `padding: var(--space-3)`. Remove heavy inline padding/margin if present.
9. The card grid layout -- if cards render in a grid, keep the existing layout. If they render as a vertical stack, that is fine (matches Stripe invoice list pattern).

**Order detail modal (if inline in renderOrders or nearby):**
10. Search for the order detail modal rendering. If it uses `showModal()`, the modal chrome will be restyled in Phase 26. For now, just ensure any inline styles within the modal content body (the part renderOrders controls) use CSS variables instead of hardcoded colors: `var(--bg-primary)` for backgrounds, `var(--border-primary)` for borders, `var(--text-primary)` for text.
11. If modal content has heavy inline background/padding styles, flatten them: `background: var(--bg-primary); padding: var(--space-4);`.

**Bulk action bar (if present in renderOrders):**
12. The floating bulk action bar (`#bulkActionBar`) at the bottom -- if it has inline gradient or heavy shadow styles, flatten to: `background: var(--bg-primary); border: 1px solid var(--border-primary); box-shadow: var(--shadow-md); border-radius: var(--radius-md);`.
  </action>
  <verify>
Open the app, navigate to Orders, switch to table view:
- Table has clean hairline borders (not heavy borders)
- Headers have subtle bg-tertiary background
- Rows have subtle alternating tint (not heavy zebra)
- Status badges use desaturated CSS classes
- Switch to card view -- cards are in a clean wrapper
- Open an order detail modal -- content uses CSS variables, no hardcoded colors
- Select multiple orders (Shift+Click) -- bulk bar is flat with subtle shadow
  </verify>
  <done>Table view uses .data-table class with refined styling, card view wrapper is clean, order detail modal content uses CSS variables, bulk action bar is flattened</done>
</task>

</tasks>

<verification>
1. Navigate to Orders page -- overall appearance matches Stripe/Linear aesthetic (flat, neutral, professional)
2. Card view: Cards are compact with hairline borders, desaturated badges, mono dollar amounts
3. Table view: Clean headers, hairline borders, subtle row separation, no heavy zebra striping
4. Filter bar: Clean inputs, consistent spacing, neutral labels
5. Button hierarchy: Only "+ New Order" is dark slate primary, all others are secondary
6. Pagination: Flat with hairline top border (from Plan 01)
7. Mobile (375px): Orders page still functional, cards stack vertically, no broken layouts
8. Navigate to Trips page, check order cards in trip detail -- inherited flat style from Plan 01
9. Navigate to Brokers page, check order cards -- inherited flat style from Plan 01
</verification>

<success_criteria>
- Orders page header, filters, and controls use the Stripe/Linear aesthetic
- Table view uses .data-table class with refined styling
- Button hierarchy follows user decision (primary for New Order only)
- AI Import button is btn-secondary (not purple)
- No functional regressions -- all buttons, filters, and interactions work unchanged
- Mobile layout intact
</success_criteria>

<output>
After completion, create `.planning/phases/21-orders-page-restyle/21-02-SUMMARY.md`
</output>
