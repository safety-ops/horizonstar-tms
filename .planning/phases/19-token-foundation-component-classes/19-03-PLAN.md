---
phase: 19-token-foundation-component-classes
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "No decorative animations play on page load (no card bounce, no row slide, no number pop, no title slide)"
    - "Spinner, toast slide-in, skeleton pulse, and progress fill still animate correctly"
    - "No hover effect uses transform scale or translateY (except functional toggles)"
    - "No card has a radial-gradient ::after pseudo-element glow"
    - "Order list items do not bounce or lift on hover"
  artifacts:
    - path: "index.html"
      provides: "Cleaned inline style block with decorative animations removed"
      contains: "animation: none"
  key_links:
    - from: "index.html decorative keyframes"
      to: "index.html animation rules"
      via: "Both keyframe AND rule must be removed/neutralized -- removing only keyframe breaks cards (Pitfall #3)"
      pattern: "animation: none"
---

<objective>
Remove all decorative animations and hover effects from the inline style block in index.html, keeping only functional animations (spinner, toast, skeleton, progress, toggle, modal overlay).

Purpose: Decorative animations are the most visible anti-Stripe pattern. Cards bouncing in, rows sliding, numbers popping, nav icons rotating on hover -- all contradict the professional, precise feel. This is the CMP-05 requirement (decorative animations removed) plus CMP-06 (subtle hover states).

Output: Cleaned index.html with ~40 decorative keyframes removed and hover effects neutralized.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-token-foundation-component-classes/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove decorative keyframes from index.html</name>
  <files>index.html</files>
  <action>
Remove (delete entirely or replace with `/* removed v1.4 */` comment) ALL of these @keyframes blocks from the inline style in index.html:

**Decorative keyframes to REMOVE (with approximate line numbers from research):**
- `slideInUp` (~line 517)
- `slideUp` (~line 1930, also ~5343 duplicate)
- `pulse` (~line 3937)
- `chatPulse` (~line 4844)
- `live-dot-breathe` (~line 6571)
- `kpi-pulse-green` (~line 6611)
- `fleet-card-enter` (~line 6926)
- `marker-pulse` (~line 7071)
- `driveTruck` (~line 7364)
- `roadMove` (~line 7392)
- `loadingBounce` (~line 7422)
- `loadingFadeInUp` (~line 7427)
- `fadeInUp` (~line 7437)
- `fadeInScale` (~line 7442)
- `slideInRight` (~line 7447)
- `slideInLeft` (~line 7452)
- `scaleIn` (~line 7457)
- `shimmer` (~line 7462) -- redundant with base.css skeleton-shimmer
- `pulse-soft` (~line 7467)
- `float` (~line 7472)
- `ripple` (~line 7477)
- `gradient-shift` (~line 7482)
- `staggerFadeIn` (~line 8611)
- `cardEnter` (~line 8622)
- `numberPop` (~line 8660)
- `titleEnter` (~line 8692)
- `rowSlideIn` (~line 8704)
- `iconBounce` (~line 8934)
- `searchPulse` (~line 8994)
- `modalContentIn` (~line 9005) -- redundant with modalSlideIn
- `badgeBounce` (~line 9066)
- `tabActivate` (~line 9087)
- `dropdownIn` (~line 9160) -- simplify: change to `from { opacity: 0 } to { opacity: 1 }` instead of removing entirely (dropdowns need a fade)

**Keyframes to KEEP (functional):**
- `spin` (lines 3966, 9541) -- spinner rotation
- `skeleton-pulse` (line 9546) -- skeleton loading
- `fadeIn` (line 1929) -- used by command palette/overlays
- `toastSlideIn` (line 8676) -- toast notifications
- `toastIn` (line 9138) -- toast entrance
- `toastOut` (line 9148) -- toast exit
- `overlayFadeIn` (line 8133) -- modal overlay (simplify: remove blur transition if present, keep opacity)
- `modalSlideIn` (line 8154) -- modal entrance
- `progressFill` (line 9100) -- progress bar
- `toggleOn` (line 9261) -- toggle switch
- `typingBounce` (line 47009) -- AI typing indicator (separate block, leave alone)
- `billingSlideUp` (line 31089) -- inside template literal, leave alone

**Simplify `pageEnter` (~line 7494):**
Change to opacity-only fade: `from { opacity: 0; } to { opacity: 1; }` -- remove any translateY.

**Simplify `overlayFadeIn` (~line 8133):**
If it has `backdrop-filter: blur(...)` in `to` state, remove the blur transition. Keep the opacity fade.

When removing a keyframe, search for WHERE the keyframe name is used (grep for the name) and ensure the animation rule referencing it is also handled in Task 2.

IMPORTANT: Do NOT touch the `<style>` block at line 47007 (AI advisor) or the billingSlideUp at line 31089 (inside template literal). Those are separate contexts.
  </action>
  <verify>
1. `grep '@keyframes cardEnter' index.html` -- should return 0 matches
2. `grep '@keyframes driveTruck' index.html` -- should return 0 matches
3. `grep '@keyframes gradient-shift' index.html` -- should return 0 matches
4. `grep '@keyframes staggerFadeIn' index.html` -- should return 0 matches
5. `grep '@keyframes spin' index.html` -- should STILL return matches (kept)
6. `grep '@keyframes toastSlideIn' index.html` -- should STILL return matches (kept)
7. `grep '@keyframes skeleton-pulse' index.html` -- should STILL return matches (kept)
8. `grep '@keyframes progressFill' index.html` -- should STILL return matches (kept)
9. `grep '@keyframes toggleOn' index.html` -- should STILL return matches (kept)
10. `grep '@keyframes typingBounce' index.html` -- should STILL return match at line 47009 (kept)
  </verify>
  <done>All ~30 decorative keyframes removed from index.html inline style block. Functional keyframes (spin, skeleton-pulse, fadeIn, toast*, overlayFadeIn, modalSlideIn, progressFill, toggleOn, pageEnter, typingBounce) preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize decorative animation rules and hover effects</name>
  <files>index.html</files>
  <action>
After removing keyframes in Task 1, neutralize the CSS RULES that reference those keyframes. This is critical -- per Pitfall #3, removing a keyframe without removing the rule that uses it causes elements to start invisible (because `animation: cardEnter ... both` with a missing keyframe = broken).

**Animation rules to neutralize (set animation: none or remove):**

1. **Card entrance** (~line 8650-8652):
   `.card { animation: cardEnter 0.5s ... }` -> `.card { animation: none; }`

2. **Stat card stagger** (~lines 8634-8639):
   `.stats-grid .stat-card:nth-child(N) { animation: ... }` -> Remove all 6 rules entirely

3. **Driver card stagger** (~lines 8641-8647):
   `.drivers-grid .driver-card:nth-child(N) { animation: ... }` -> Remove all 6 rules entirely

4. **Number pop** (~lines 8655-8658):
   `.stat-card .value { animation: numberPop ... }` -> Remove or set `animation: none`

5. **Page title** (~lines 8688-8690):
   `.page-title { animation: titleEnter ... }` -> Remove or set `animation: none`

6. **Table row slide-in** (~lines 8709-8722):
   `tbody tr { animation: rowSlideIn ... }` -> Set `animation: none`
   Remove all `tbody tr:nth-child(N) { animation-delay: ... }` rules (lines 8713-8722)

7. **Table row hover scale** (~lines 8729-8733):
   `tbody tr:hover { transform: scale(1.005); }` -> Remove the transform and z-index. Keep the transition rule but change it to just `transition: background-color 150ms ease;`

8. **Card hover glow pseudo-element** (~lines 8786-8799):
   `.card::after { ... radial-gradient ... }` -> Set `display: none` or remove the entire `.card::after` and `.card:hover::after` blocks

9. **Order list item spring/hover** (~lines 8827-8845):
   `.order-list-item { ... transition: transform 180ms cubic-bezier(0.34, 1.56, 0.64, 1) ... }` -> Change transition to `transition: background-color 150ms ease, border-color 150ms ease, box-shadow 150ms ease;`
   Remove `will-change: transform;`
   `.order-list-item:hover { transform: translateY(-3px) scale(1.004); ... box-shadow: 0 14px 32px ... }` -> Remove `transform`. Change `box-shadow` to `var(--shadow-card-hover)`. Remove the `!important` on background if possible (or change to `background: var(--bg-card-hover) !important`).

10. **Action button lift** (~lines 8753-8763):
    `.action-btn:hover { transform: translateY(-2px) scale(1.02); }` -> Remove transform
    `.action-btn:active { transform: translateY(0) scale(0.98); }` -> Remove transform

11. **Nav icon rotate** (~lines 8771-8773):
    `.nav-item:hover .icon { transform: scale(1.15) rotate(-3deg); }` -> Remove transform or set `transform: none`
    `.nav-item.active .icon { transform: scale(1.1); }` -> Remove or set `transform: none`

12. **Stat card icon bounce** (~lines 8930-8932):
    `.stat-card:hover .stat-icon { animation: iconBounce ... }` -> Remove

13. **Driver avatar rotate** (~lines 8941-8948):
    `.driver-card:hover .driver-avatar { transform: scale(1.1) rotate(-5deg); }` -> Remove transform and box-shadow change

14. **Badge hover** (~lines 8951-8958):
    `.badge:hover { transform: translateY(-1px); filter: brightness(1.05); }` -> Remove transform and filter. Keep `transition: background-color 150ms ease;`

15. **Animate utility classes** (~lines 8596-8604):
    `.animate-scale`, `.animate-slide-right`, `.hover-lift`, `.hover-glow:hover` -> Remove all these utility classes entirely

16. **Icon button hover** (~lines 8737-8741):
    `.btn-icon:hover .icon` transition with spring -> Remove or simplify to no transform

17. **Order card link font-weight** (~line 8874):
    `.order-card-link { font-weight: 700; }` -> Change to `font-weight: 600` (weight cap)

18. **Order card amount font-weight** (~line 8911):
    `.order-card-amount { font-weight: 700; }` -> Change to `font-weight: 600`

For ALL hover states modified: the replacement should be either `background-color` change only, or removal. Stripe/Linear hover = subtle background shift, nothing else.

IMPORTANT: Do NOT touch the `.orders-list-shell` hover rules (lines 8802-8818) -- they already neutralize pseudo-elements and set `transform: none`.
IMPORTANT: Do NOT touch order-card layout rules (lines 8847-8927) -- those are functional layout, not decorative.
IMPORTANT: Do NOT change the `.toast` rule at line 8672 -- toastSlideIn is kept.
  </action>
  <verify>
1. `grep 'animation: cardEnter' index.html` -- should return 0 matches
2. `grep 'animation: rowSlideIn' index.html` -- should return 0 matches
3. `grep 'animation: numberPop' index.html` -- should return 0 matches
4. `grep 'animation: titleEnter' index.html` -- should return 0 matches
5. `grep 'transform: scale(1.005)' index.html` -- should return 0 matches (row hover removed)
6. `grep 'transform: translateY(-3px)' index.html` -- should return 0 matches (order hover removed)
7. `grep 'transform: scale(1.15) rotate' index.html` -- should return 0 matches (nav icon removed)
8. `grep 'radial-gradient.*card' index.html` -- should return 0 matches in the hover glow block
9. `grep 'animation: toastSlideIn' index.html` -- should STILL return match (kept)
10. Open index.html in browser -- cards should appear instantly (no bounce-in), rows should appear instantly (no slide), page should feel static and professional
11. Hover over a card -- no glow, no lift, just subtle background shift
12. Hover over a nav item -- icon should NOT rotate or scale
  </verify>
  <done>All decorative animation rules neutralized. Cards appear instantly, rows appear instantly, no hover transforms, no glow pseudo-elements. Toast, spinner, skeleton, progress, toggle, and modal animations still work. The app feels static and professional on every page.</done>
</task>

</tasks>

<verification>
1. Open index.html -- dashboard should load with NO entrance animations (cards just appear, no bounce)
2. Navigate to any page with a table -- rows should appear instantly (no staggered slide-in)
3. Hover over cards -- no glow effect, no lift, just subtle background change
4. Hover over nav items -- icons should NOT rotate or scale
5. Toast notifications still slide in from the right
6. Loading spinner still spins
7. Skeleton loading states still pulse
8. Modals still fade in smoothly
</verification>

<success_criteria>
- All ~30 decorative keyframes removed from index.html
- All animation rules referencing removed keyframes neutralized (animation: none or deleted)
- All hover transforms removed (no scale, no translateY, no rotate on hover)
- Card ::after radial-gradient glow removed
- Functional animations preserved: spin, skeleton-pulse, fadeIn, toast*, overlayFadeIn, modalSlideIn, progressFill, toggleOn, pageEnter (opacity-only)
- Font weights in inline block capped at 600
- Requirements covered: CMP-05, CMP-06
</success_criteria>

<output>
After completion, create `.planning/phases/19-token-foundation-component-classes/19-03-SUMMARY.md`
</output>
