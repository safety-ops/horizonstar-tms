---
phase: 19-token-foundation-component-classes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/variables.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "Opening any page shows neutral slate-gray surfaces instead of warm zinc tones"
    - "Body text renders in Inter, not IBM Plex Sans or Space Grotesk"
    - "All primary buttons reference dark slate token (#0f172a)"
    - "Shadows are near-invisible at 3 depth levels"
    - "Border radius is tighter (cards 8px, inputs 6px, modals 12px)"
    - "Font weights capped at 600 (no 700 or 800)"
  artifacts:
    - path: "assets/css/variables.css"
      provides: "Complete Stripe/Linear token layer"
      contains: "--btn-primary-bg: #0f172a"
    - path: "index.html"
      provides: "Neutralized inline style block (no competing fonts/colors)"
      contains: "font-family: var(--font-sans)"
  key_links:
    - from: "assets/css/variables.css"
      to: "index.html inline :root"
      via: "Inline block must NOT re-declare tokens that conflict with variables.css"
      pattern: "No --font-display, no --font-body, no rem text sizes in inline :root"
---

<objective>
Swap all CSS token values in variables.css to the Stripe/Linear Tailwind Slate palette and simultaneously neutralize the competing inline style block at index.html line 35 that overrides variables.css via cascade order.

Purpose: This is the foundation -- 60-70% of the visual shift happens here because every component already references these CSS variables. The inline style block MUST be updated simultaneously (Pitfall #1: it comes after variables.css in the document and wins by cascade).

Output: Updated variables.css with complete Stripe/Linear tokens, neutralized inline :root block in index.html.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-token-foundation-component-classes/19-RESEARCH.md
@assets/css/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace variables.css with complete Stripe/Linear token values</name>
  <files>assets/css/variables.css</files>
  <action>
Replace the entire contents of variables.css with the complete token set from the research file (section "Complete variables.css After Token Swap", lines 527-683 of 19-RESEARCH.md).

Key changes:
- Colors: Zinc grays (#f5f5f7, #111827) to Slate (#f8fafc, #0f172a)
- Borders: rgba(0,0,0,0.08) to solid #e2e8f0, add --border-strong: #cbd5e1
- Shadows: Flatten to 3 levels (xs/sm/md), collapse lg/xl to md, default to xs
- Radius: 12px->8px, 8px->6px, 16px->12px, 20px->12px
- Weights: --weight-bold: 600 (was 700), --weight-heavy: 600 (was 800)
- Transition: Explicit properties (not `all`), 150ms ease
- Ease: --ease-spring and --ease-bounce set to `ease` (neutralized)
- Duration: --duration-slow from 400ms to 250ms
- NEW tokens: --btn-primary-bg (#0f172a), --btn-primary-color (#ffffff), --btn-primary-hover (#1e293b), --border-strong, --focus-ring, --focus-border, --tracking-wide
- Semantic colors: Slightly deeper hues, dim opacity from 12% to 8%
- Gradient vars: All set to flat solid color values (no linear-gradient)

The complete replacement content is in the research file. Use it verbatim.

Do NOT remove the .dark-theme section if one exists. Do NOT add !important anywhere.
  </action>
  <verify>
1. `grep '#0f172a' assets/css/variables.css` returns matches for --text-primary and --btn-primary-bg
2. `grep '#f8fafc' assets/css/variables.css` returns match for --bg-primary
3. `grep 'weight-bold: 600' assets/css/variables.css` returns match
4. `grep 'weight-heavy: 600' assets/css/variables.css` returns match
5. `grep 'radius: 8px' assets/css/variables.css` returns match for --radius
6. `grep 'ease-spring: ease' assets/css/variables.css` returns match
7. `grep 'btn-primary-bg' assets/css/variables.css` returns match
8. No `700` or `800` appears in any weight variable
  </verify>
  <done>variables.css contains the complete Stripe/Linear token set with Tailwind Slate palette, 3-level shadows, tighter radius, capped weights, dark slate button tokens, and no decorative animation easings.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize competing declarations in index.html inline style block</name>
  <files>index.html</files>
  <action>
Edit the inline `<style>` block starting at line 35 of index.html. The `:root` section (lines 36-80) overrides variables.css. Make these specific changes:

1. **Remove competing font declarations** (lines 48-51):
   - Delete `--font-display: 'Space Grotesk'...`
   - Delete `--font-body: 'IBM Plex Sans'...`
   - Delete the `--font-mono: 'JetBrains Mono'...` override (variables.css has the correct value)

2. **Remove rem-based text size overrides** (lines 37-46):
   - Delete ALL of these: `--text-xs` through `--text-4xl`
   - These override the px-based scale in variables.css with larger rem values that don't match the Stripe aesthetic

3. **Remove extended indigo spectrum** (lines 53-63):
   - Delete `--primary-50` through `--primary-900` -- unused by the restyle

4. **Remove accent cyan** (lines 65-67):
   - Delete `--accent` and `--accent-light` -- not part of the Stripe palette

5. **Remove gradient/glow declarations** (lines 69-79):
   - Delete `--gradient-surface`, `--gradient-glow`, `--gradient-mesh`
   - Delete `--glow-primary`, `--glow-accent`

6. **Fix body font-family** (line 89):
   - Change `font-family: var(--font-body)` to `font-family: var(--font-sans)`

7. **Fix heading font declarations** (lines 101-106):
   - Change `font-family: var(--font-display)` to `font-family: var(--font-sans)`
   - Change `font-weight: 700` to `font-weight: 600`

8. **Fix monospace reference** (lines 109-112):
   - Change `font-family: var(--font-mono)` -- this is fine, keep it (variables.css has the correct --font-mono)

After these changes, the inline `:root` block should be either empty (and removable) or contain only non-conflicting declarations. The body and heading rules should reference `var(--font-sans)` and use weight 600.

IMPORTANT: Do NOT touch anything below line ~135 in this task. The animation cleanup is a separate plan.
IMPORTANT: Do NOT remove the `<style>` tag itself -- it contains thousands of lines of CSS below the `:root` block.
IMPORTANT: Keep the `.ui-icon`, `.ui-icon-inline`, `.ui-icon-label` rules (lines 114-134) -- those are functional.
  </action>
  <verify>
1. `grep 'Space Grotesk' index.html | head -5` -- should return 0 matches in the inline style block (may appear in Google Fonts link, that's fine)
2. `grep 'IBM Plex' index.html | head -5` -- should return 0 matches in the inline style block
3. `grep 'font-body' index.html | head -5` -- should return 0 matches (variable eliminated)
4. `grep 'font-display' index.html | head -5` -- should return 0 matches as a CSS variable (may appear as a comment)
5. `grep 'font-family: var(--font-sans)' index.html | head -3` -- should match in body rule
6. `grep '\-\-primary-50' index.html` -- should return 0 matches
7. `grep 'gradient-mesh' index.html` -- should return 0 matches
8. Open index.html in browser -- text should render in Inter, not IBM Plex Sans
  </verify>
  <done>The inline style block no longer competes with variables.css. Body text uses Inter via var(--font-sans), headings use Inter with weight 600, no rem text size overrides, no indigo spectrum, no gradient/glow variables.</done>
</task>

</tasks>

<verification>
1. Open index.html in a browser -- page background should be a cool slate-gray (#f8fafc), not warm zinc (#f5f5f7)
2. Text should render in Inter font (not IBM Plex Sans or Space Grotesk)
3. Inspect any card element -- border-radius should compute to 8px
4. DevTools: check computed value of `--btn-primary-bg` on :root -- should be #0f172a
5. DevTools: check `--shadow-card` resolves to the xs value (0 1px 2px...)
</verification>

<success_criteria>
- variables.css contains complete Stripe/Linear token set (Slate palette, 3 shadow levels, 6px/8px/12px radius, 600 max weight, dark slate button tokens)
- Inline style block `:root` no longer overrides variables.css tokens
- Body renders in Inter font
- No visual breakage on any page (functional layout preserved)
- Requirements covered: TOK-01, TOK-02, TOK-03, TOK-04, TOK-05
</success_criteria>

<output>
After completion, create `.planning/phases/19-token-foundation-component-classes/19-01-SUMMARY.md`
</output>
