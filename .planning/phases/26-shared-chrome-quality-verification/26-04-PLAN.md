---
phase: 26-shared-chrome-quality-verification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [index.html, assets/js/auth.js]
autonomous: true

must_haves:
  truths:
    - "Login page has light background using var(--bg-primary), not #030808"
    - "No particle rain container or particle generation loop exists"
    - "No ::before gradient overlay or ::after grid pattern on login-container"
    - "Login form card uses var(--bg-secondary) background with 12px border-radius"
    - "Login form inputs use CSS variable colors, not rgba(255,255,255,*)"
    - "Login page is a clean centered form on a simple light background"
  artifacts:
    - path: "index.html"
      provides: "Restyled login page CSS"
      contains: "var\\(--bg-primary\\)|var\\(--bg-secondary\\)"
    - path: "assets/js/auth.js"
      provides: "Login HTML without particle generation"
  key_links:
    - from: "auth.js login template"
      to: "index.html login CSS"
      via: "CSS class names"
      pattern: "login-container|login-form-card"
---

<objective>
Restyle the login page from a dark themed, particle-animated page to a clean flat form on a simple light background. Remove all decorative elements (particles, gradients, grid patterns).

Purpose: The login page is the first impression -- dark particle effects clash with the Stripe/Linear aesthetic applied to every other page.
Output: Clean light login page with centered form card, no decorative animations.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-shared-chrome-quality-verification/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle login page CSS</name>
  <files>index.html</files>
  <action>
Find the login CSS block starting at `.login-container` around line 3229 and extending to approximately line 3678.

**login-container (~line 3229):**
- `background: #030808` -> `background: var(--bg-primary)`
- Remove the `min-height: 100vh` if display:flex + align-items:center already handles centering (keep min-height if needed for full viewport)
- Keep `display: flex; align-items: center; justify-content: center`

**Remove ::before pseudo-element (~line 3238):**
Delete the entire `.login-container::before` rule (gradient overlay). Set `content: none` or delete the rule entirely.

**Remove ::after pseudo-element (~line 3248):**
Delete the entire `.login-container::after` rule (grid pattern overlay).

**Remove .login-particles (~line 3260):**
Delete the entire `.login-particles` rule and ALL `.login-particle` rules (including nth-child animations, keyframes for particleFloat, particlePulse, etc.). These may span 50+ lines.

**login-form-card (~line 3453):**
- `background: #0a1014` -> `background: var(--bg-secondary)`
- `border-radius: 24px` -> `border-radius: 12px`
- `border: 1px solid rgba(255,255,255,0.08)` -> `border: 1px solid var(--border)`
- `box-shadow`: Replace any heavy/glow shadow with `var(--shadow-md)`
- `max-width`: Keep at ~420px or similar (reasonable form width)

**login-form-card h2 (~line 3465):**
- Remove any gradient text effect (`-webkit-background-clip: text; -webkit-text-fill-color: transparent`)
- `color: var(--text-primary); font-weight: 600; font-size: 24px`

**login-form-card .subtitle (~line 3477):**
- `color: rgba(255,255,255,0.5)` -> `color: var(--text-secondary)`

**Form group labels (~line 3490):**
- `color: rgba(255,255,255,0.7)` -> `color: var(--text-secondary); font-weight: 600; font-size: 12px`

**Form group inputs (~line 3501):**
- `background: rgba(255,255,255,0.05)` -> `background: var(--bg-primary)`
- `border: 1px solid rgba(255,255,255,0.1)` -> `border: 1px solid var(--border)`
- `color: white` -> `color: var(--text-primary)`
- Focus state: `border-color: var(--primary)` (keep if already correct)
- `::placeholder color: rgba(255,255,255,0.3)` -> `color: var(--text-muted)`
- Height: `min-height: 38px` or `height: 42px` — reasonable touch target

**Submit button (~line 3530):**
- Convert to match the established btn-primary pattern: `background: var(--slate-900); color: white; border-radius: 8px; font-weight: 600`
- Remove any gradient background
- Hover: `background: var(--slate-800)` — subtle darkening, no transform

**Form footer / links (~line 3550):**
- `color: rgba(255,255,255,0.5)` -> `color: var(--text-secondary)`
- Link color: `color: var(--primary)` on hover

**Language toggle (~line 3567) and any feature cards / hero text:**
- Convert all rgba(255,255,255,*) -> CSS variables
- Convert all hardcoded dark hex -> CSS variables

**Remove ALL keyframes** related to login animations (particleFloat, particlePulse, loginGlow, etc.). Search for `@keyframes particle` and `@keyframes login` within the login CSS section and delete them.

**Do NOT** change form submission logic, password reset flow, or PIN lockout functionality.
  </action>
  <verify>Search login CSS section (lines 3229-3678) for: #030808, #0a1014, rgba(255,255,255, particleFloat, particlePulse, login-particle, gradient-primary. All should return zero matches. Confirm login-container uses var(--bg-primary) and login-form-card uses var(--bg-secondary).</verify>
  <done>Login CSS uses light theme variables throughout. No particle CSS, no gradient overlays, no grid pattern. Form card has 12px radius, clean shadow, CSS variable colors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove particle generation from auth.js</name>
  <files>assets/js/auth.js</files>
  <action>
Find the particle generation code in auth.js around lines 165-170. It looks like:

```javascript
let particlesHtml = '<div class="login-particles">';
for (let i = 0; i < 30; i++) {
  particlesHtml += '<div class="login-particle"></div>';
}
particlesHtml += '</div>';
```

**Delete** the entire particle generation loop (the `let particlesHtml` declaration, the for loop, and the closing `</div>` append).

Then find where `${particlesHtml}` is interpolated into the login template (around line 259) and **remove** that interpolation.

Also scan the login HTML template in auth.js for any remaining hardcoded color values in inline styles:
- `color:white` or `color: white` -> `color:var(--text-primary)`
- `color:#xxx` dark-theme values -> CSS variable equivalents
- `background:#030808` or similar -> remove or use CSS variable

**Do NOT** change the form fields, validation logic, submit handlers, password reset template, or PIN lockout template.
  </action>
  <verify>Search auth.js for: login-particles, login-particle, particlesHtml. All should return zero matches. Confirm the login form still renders (search for "login-container" to verify the template is intact).</verify>
  <done>auth.js has no particle generation code. Login template renders a clean form without decorative elements. No hardcoded dark-theme colors in inline styles.</done>
</task>

</tasks>

<verification>
1. Open the app while logged out — should see a clean light background with a centered white form card
2. No floating particles or animated elements visible
3. Form inputs should have light backgrounds with visible borders
4. Submit button should be dark slate (btn-primary style)
5. Form should still accept credentials and log in successfully
6. Password reset link should still work
</verification>

<success_criteria>
- Login page background is var(--bg-primary)
- Form card is var(--bg-secondary) with 12px radius
- Zero particle-related CSS or JS code
- Zero rgba(255,255,255,*) in login CSS
- Zero gradient overlays or grid patterns
- Login functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/26-shared-chrome-quality-verification/26-04-SUMMARY.md`
</output>
