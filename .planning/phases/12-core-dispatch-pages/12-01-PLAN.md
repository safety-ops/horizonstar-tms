---
phase: 12-core-dispatch-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [assets/css/design-system.css]
autonomous: true

must_haves:
  truths:
    - "Sticky action columns have matching hover background (light and dark themes)"
    - "Summary row, info bar, and page-specific component classes exist in design-system.css"
    - "No duplicate class definitions introduced (append only)"
  artifacts:
    - path: "assets/css/design-system.css"
      provides: "sticky-col, summary-row, and page-component CSS classes"
      contains: ".sticky-col"
  key_links:
    - from: "assets/css/design-system.css"
      to: "index.html"
      via: "CSS classes used in render functions"
      pattern: "sticky-col|summary-row"
---

<objective>
Add page-component CSS classes to design-system.css that Phase 12 pages require but don't yet exist. This is the CSS foundation that all subsequent Phase 12 plans depend on.

Purpose: Production render functions already use `sticky-col` class on `<th>` and `<td>` elements but the CSS rule doesn't exist in design-system.css, causing them to not actually stick. Similarly, summary-row and other page-level component styles from the mockup shared.css need to be ported.

Output: Updated design-system.css with sticky-col, summary-row, and any other page-component classes needed by Pages 12-02 through 12-05.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/css/design-system.css
@mockups/web-tms-redesign/shared.css
@mockups/web-tms-redesign/trips.html (lines 18-61 for sticky-col and summary-row styles)
@mockups/web-tms-redesign/dashboard.html (lines 200-400 for stat card patterns)
@.planning/phases/12-core-dispatch-pages/12-CONTEXT.md
@.planning/phases/12-core-dispatch-pages/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sticky-col and summary-row CSS classes to design-system.css</name>
  <files>assets/css/design-system.css</files>
  <action>
  Append the following CSS classes after the existing component styles in design-system.css (after the last rule, before any closing comments):

  1. **Sticky action column** (from mockup trips.html lines 18-33):
     ```css
     /* Sticky Action Column */
     .sticky-col {
       position: sticky;
       right: 0;
       background: var(--bg-card);
       border-left: 1px solid var(--border);
       z-index: 1;
     }

     thead .sticky-col {
       z-index: 2;
       background: var(--bg-card);
     }

     .table tbody tr:hover .sticky-col {
       background: var(--bg-card-hover);
     }
     ```

  2. **Summary row / info bar** (from mockup trips.html lines 34-61):
     ```css
     /* Summary Row / Info Bar */
     .summary-row {
       background: var(--bg-elevated);
       padding: 16px 20px;
       border-radius: var(--radius-lg);
       display: flex;
       gap: 24px;
       flex-wrap: wrap;
       font-size: var(--text-sm);
     }

     .summary-item {
       display: flex;
       align-items: center;
       gap: 8px;
     }

     .summary-label {
       color: var(--text-secondary);
       font-weight: var(--weight-medium);
     }

     .summary-value {
       color: var(--text-primary);
       font-weight: var(--weight-bold);
       font-family: var(--font-mono);
     }

     .summary-value.green { color: var(--green); }
     .summary-value.red { color: var(--red); }
     .summary-value.blue { color: var(--blue); }
     .summary-value.amber { color: var(--amber); }
     ```

  3. **Metric cell** (reusable for dashboard Quick View, KPI grids, financial breakdowns):
     ```css
     /* Metric Cell (for dashboard/detail grids) */
     .metric-cell {
       text-align: center;
       padding: 16px;
       background: var(--bg-app);
       border-radius: var(--radius);
       border: 1px solid var(--border);
     }

     .metric-cell .metric-label {
       font-size: var(--text-xs);
       color: var(--text-muted);
       text-transform: uppercase;
       letter-spacing: 0.5px;
       margin-bottom: 6px;
     }

     .metric-cell .metric-value {
       font-size: var(--text-2xl);
       font-weight: var(--weight-heavy);
       font-family: var(--font-mono);
     }

     .metric-cell .metric-hint {
       font-size: 10px;
       color: var(--text-muted);
       margin-top: 4px;
     }
     ```

  4. **Section header with colored underline** (used across dashboard sections):
     ```css
     /* Section Header with Color Accent */
     .section-title {
       margin-bottom: 16px;
       padding-bottom: 8px;
       border-bottom: 2px solid var(--border);
       font-size: var(--text-base);
       font-weight: var(--weight-bold);
     }

     .section-title.green { color: var(--green); border-color: var(--green); }
     .section-title.red { color: var(--red); border-color: var(--red); }
     .section-title.blue { color: var(--blue); border-color: var(--blue); }
     .section-title.amber { color: var(--amber); border-color: var(--amber); }
     .section-title.purple { color: var(--purple); border-color: var(--purple); }
     ```

  IMPORTANT: Only append new classes. Do NOT modify any existing CSS rules. Do NOT duplicate any class already defined (check existing content first). Use design system tokens exclusively (no hardcoded hex colors, font sizes, or spacing values).
  </action>
  <verify>
  1. Grep for `.sticky-col` in design-system.css — should find the new rules
  2. Grep for `.summary-row` in design-system.css — should find the new rules
  3. Grep for `.metric-cell` in design-system.css — should find the new rules
  4. Grep for `.section-title` in design-system.css — should find the new rules
  5. Grep for any hardcoded hex colors (#) in the newly added lines — should find NONE
  6. Verify no duplicate class definitions by checking the file doesn't define the same class twice
  </verify>
  <done>
  design-system.css contains sticky-col (with hover state), summary-row, metric-cell, and section-title CSS classes, all using design system tokens exclusively, appended without modifying existing rules.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'sticky-col' assets/css/design-system.css` returns 3+ (the class + thead + hover rules)
2. `grep -c 'summary-row\|summary-item\|summary-label\|summary-value' assets/css/design-system.css` returns 4+
3. `grep -c 'metric-cell\|metric-label\|metric-value\|metric-hint' assets/css/design-system.css` returns 4+
4. `grep -c 'section-title' assets/css/design-system.css` returns 6+ (base + 5 color variants)
5. No hardcoded hex colors in newly added lines
</verification>

<success_criteria>
- design-system.css has all 4 new component class groups appended
- All new classes use CSS variable tokens only (no hex colors)
- Existing design-system.css rules are untouched
- Sticky-col includes both light and dark theme support via CSS variables
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-dispatch-pages/12-01-SUMMARY.md`
</output>
