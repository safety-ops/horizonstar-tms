---
phase: 20-dashboard-restyle
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Sidebar cards (Needs Action, Quick Actions, Suggested Actions, Payment Collection) use flat surfaces with consistent neutral styling"
    - "Analytics section (Other Metrics, Expenses, Fuel, KPIs, Top Performers, Cost Trends) displays with flat styling -- no colored section header borders, no gradient fills"
    - "All hardcoded hex colors in right column and analytics section are replaced with CSS variable references"
    - "Analytics section remains collapsible via click toggle"
    - "renderSuggestedActions uses CSS variables instead of hardcoded hex colors"
  artifacts:
    - path: "index.html"
      provides: "Fully restyled renderDashboard bottom half + restyled renderSuggestedActions"
  key_links:
    - from: "index.html renderDashboard right column"
      to: "assets/css/base.css"
      via: "CSS class references (card, section-header, stat-card-label, stat-card-value)"
      pattern: "section-header|stat-card-label|stat-card-value|text-label|text-value"
---

<objective>
Restyle the dashboard right column (sidebar cards) and collapsible analytics section to the Stripe/Linear aesthetic.

Purpose: Complete the dashboard restyle by transforming the sidebar and analytics from hardcoded-color inline styles to clean flat surfaces using CSS classes and token references.
Output: Fully restyled renderDashboard bottom half (right column + analytics) and restyled renderSuggestedActions function.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dashboard-restyle/20-RESEARCH.md
@.planning/phases/20-dashboard-restyle/20-CONTEXT.md
@.planning/phases/20-dashboard-restyle/20-01-SUMMARY.md
@assets/css/base.css
@assets/css/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle right column sidebar cards</name>
  <files>index.html</files>
  <action>
Rewrite the right column portion of renderDashboard (lines ~17507-17544 after Plan 01 changes) and the renderSuggestedActions function (line 10445).

**Needs Action card** (currently lines 17509-17517):
- Replace header with section-header pattern: `<div class="section-header"><h3 class="section-title">Needs Action</h3><span class="badge-xs" style="background:var(--red-dim);color:var(--red)">{count} items</span></div>`
- Remove `[[icon:alert]]` from title.
- Restyle needsActionHtml items (lines 17377-17383): replace inline styles with CSS classes.
  - Each item: use `.card-row` class (already in base.css) instead of inline flex/padding/border.
  - Icon span: `style="color:var(--amber)"` (or --red/--blue as appropriate) instead of hardcoded hex. Replace `color:'+ item.color +'` with a mapping: `#ef4444` -> `var(--red)`, `#f59e0b` -> `var(--amber)`, `#3b82f6` -> `var(--blue)`.
  - Text: use `class="text-value text-truncate"` for the title, `class="text-label"` style for the sub text (but lowercase, so use inline `font-size:11px;color:var(--text-muted)` or a small span).
  - Remove `onmouseover`/`onmouseout` background change handlers. Add `class="card-row"` which handles hover via CSS if needed.
  - Remove chevron span or keep it simple with `color:var(--text-muted)`.
- "All caught up" empty state: replace inline styles with CSS. Use `color:var(--text-muted)`.

**Quick Actions card** (lines 17519-17527):
- Replace header with section-header pattern (no border-bottom needed inside card, so use `style="border-bottom:none;padding:14px 16px 8px"` or simpler: just a title div).
- Actually keep it simple: `<h3 class="section-title" style="margin:0 0 var(--space-3)">Quick Actions</h3>`
- Remove `[[icon:bolt]]` from title.
- The "New Order" button uses `background:#3b82f6;border-color:#3b82f6` inline -> replace with `class="btn btn-primary"` and add `style="width:100%;justify-content:center;background:var(--blue);border-color:var(--blue)"` (to differentiate from default dark slate primary).
- Keep all button onclick handlers unchanged.

**renderSuggestedActions** (line 10445, ~60 lines):
- Replace hardcoded hex colors in the suggestions array items:
  - `color: '#f59e0b'` -> `color: 'var(--amber)'`
  - `color: '#3b82f6'` -> `color: 'var(--blue)'`
  - `color: '#ef4444'` -> `color: 'var(--red)'`
  - `color: '#a855f7'` -> `color: 'var(--purple)'`
- Restyle the returned HTML (around line 10502):
  - Replace `style="padding:20px"` with `style="padding:14px 16px"`.
  - Replace title: remove `color:#f59e0b` from h3. Use `<h3 class="section-title" style="margin:0 0 var(--space-3)">Suggested Actions</h3>`. Remove `[[icon:magic]]`.
  - Each suggestion item: replace inline styles with card-row-like pattern. Use `color:var(--text-secondary)` for icon color fallback.
  - Remove any `font-weight:700` -- use 600 max.

**Payment Collection card** (lines 17532-17542):
- Replace `color:#0ea5e9` on h3 with no color override (use section-title class).
- Remove `[[icon:dollar]]` icon.
- Replace header: `<h3 class="section-title" style="margin:0 0 var(--space-3)">Payment Collection</h3>`
- Replace hardcoded hex colors in payment type rows:
  - `#047857` -> `var(--green)`, `#15803d` -> `var(--green)`
  - `#b45309` -> `var(--amber)`
  - `#1d4ed8` -> `var(--blue)`
  - `#7c3aed` -> `var(--purple)`
  - `#bbf7d0` -> `var(--green-dim)` (COP row background)
- Replace `font-weight:700` with `font-weight:600` on all amount spans.
- Replace `border-radius:8px` with `border-radius:var(--radius-sm)` on payment rows.
  </action>
  <verify>
Open dashboard in browser:
1. Right column cards all have flat neutral styling
2. Needs Action items are clickable with no hardcoded hex colors
3. Quick Actions buttons render correctly
4. Payment Collection rows use CSS variables for colors
5. Suggested Actions card uses flat styling (no amber title)
6. No font-weight above 600 in sidebar elements
  </verify>
  <done>All right column sidebar cards restyled to flat treatment. renderSuggestedActions uses CSS variables. No hardcoded hex colors, no font-weight 700, no gradient fills in sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle collapsible analytics section</name>
  <files>index.html</files>
  <action>
Rewrite the analytics section of renderDashboard (lines ~17548-17702 after Plan 01 changes). The analytics section is inside `<div class="analytics-content hidden">`. Keep the collapsible toggle mechanism unchanged.

**Analytics toggle header** (lines 17549-17553):
- Replace `style="padding:16px 20px"` with `class="section-header"` and add `style="cursor:pointer"` and keep onclick.
- Use `<h3 class="section-title">Detailed Analytics</h3>` instead of inline h3.
- Remove `[[icon:chart]]` icon.
- Keep toggle arrow span.

**Other Metrics card** (lines 17560-17570):
- Replace header `color:#0ea5e9;border-bottom:2px solid #0ea5e9` with just `class="section-title"` in a section-header div. Use `<div class="section-header"><h3 class="section-title">Other Metrics</h3></div>`.
- Replace `color:var(--text-secondary)` inline -> fine to keep, it's already a CSS variable.
- Replace Most Profitable Trip box: `background:#d1fae5` -> `background:var(--green-dim)`, all `color:#047857` -> `color:var(--green)`, `font-weight:700` -> `font-weight:600`.
- Replace Least Profitable Trip box: `background:#fee2e2` -> `background:var(--red-dim)`, all `color:#dc2626` -> `color:var(--red)`, `font-weight:700` -> `font-weight:600`.
- Replace `font-size:18px;font-weight:700` on trip numbers with `font-size:var(--text-lg);font-weight:600`.

**Direct Trip Expenses card** (lines 17572-17585):
- Replace header `color:#ef4444;border-bottom:2px solid #ef4444` with section-header pattern (no color).
- Progress bars: keep the dim background + colored fill pattern but replace hex colors:
  - `#ef4444` -> `var(--red)`
  - `#f59e0b` -> `var(--amber)`
  - `#8b5cf6` -> `var(--purple)`
  - `#64748b` -> `var(--text-secondary)`
  - `#e2e8f0` -> `var(--bg-tertiary)`
- Total line: `font-weight:700` -> `font-weight:600`, `color:#dc2626` -> `color:var(--red)`.

**Fuel Tracking card** (lines 17589-17597):
- Replace header with section-header pattern, remove color.
- Fuel stat boxes: replace `border-left:4px solid #f59e0b` with `border-left:2px solid var(--amber)`, and `border-left:4px solid #3b82f6` with `border-left:2px solid var(--blue)`.
- Replace all hardcoded colors: `#92400e`/`#a16207` -> `var(--amber)`, `#1d4ed8`/`#2563eb` -> `var(--blue)`.
- Replace `font-size:24px;font-weight:700` with `font-size:var(--text-xl);font-weight:600;font-family:var(--font-mono)`.
- Warning box: `background:#fef3c7` -> `background:var(--amber-dim)`, `color:#92400e` -> `color:var(--amber)`.

**KPIs card** (lines 17602-17612):
- Replace header with section-header pattern, remove `color:#8b5cf6;border-bottom:2px solid #8b5cf6`.
- Metric cells: replace `background:var(--surface-card)` with `background:var(--bg-secondary)`, `border-radius:8px` with `border-radius:var(--radius-sm)`.
- Labels: `color:#64748b` -> `color:var(--text-muted)`.
- Values: `font-size:18px;font-weight:700;color:#1e293b` -> `font-size:var(--text-lg);font-weight:600;color:var(--text-primary);font-family:var(--font-mono)`.
- Conditional profit colors: `#047857` -> `var(--green)`, `#dc2626` -> `var(--red)`.
- Truck Gross: `color:#0ea5e9` -> `color:var(--blue)`.

**Top Performers card** (lines 17614-17622):
- Replace header with section-header pattern, remove `color:#10b981;border-bottom:2px solid #10b981`.
- Performer boxes: keep dim backgrounds but replace all hex colors:
  - `#047857` -> `var(--green)`, `#dc2626` -> `var(--red)`, `#1d4ed8` -> `var(--blue)`, `#64748b` -> `var(--text-secondary)`
  - `background:var(--surface-elevated)` -> `background:var(--bg-secondary)`
- Values: `font-size:24px;font-weight:700` -> `font-size:var(--text-xl);font-weight:600;font-family:var(--font-mono)`.
- Labels (11px): already fine, just fix any hardcoded colors.

**Cost Trends section** (lines 17626-17699):
- Replace header: remove `color:#64748b;border-bottom:2px solid #64748b`. Use section-header pattern.
- Trend cards: replace `border-radius:12px` with `border-radius:var(--radius)` (8px), keep `border:1px solid var(--border)`.
- Replace `background:var(--surface-card)` with `background:var(--bg-card)`.
- Replace `color:#334155` (category names) with `color:var(--text-primary)`.
- Trend badge: keep dynamic background/color computation but replace hex values:
  - `#dc2626` -> `var(--red)`, `#16a34a` -> `var(--green)`, `#64748b` -> `var(--text-secondary)`
  - `#fee2e2` -> `var(--red-dim)`, `#dcfce7` -> `var(--green-dim)`, `#f1f5f9` -> `var(--bg-secondary)`
  - `border-radius:12px` -> `border-radius:var(--radius-sm)`
- Sparkline colors in `categoryColors` object: `#ef4444` -> keep as hex (SVG inline colors are fine for sparklines since they're not themed).
- Bottom stats: `color:#94a3b8` -> `color:var(--text-muted)`.
- Replace `font-weight:700` -> `font-weight:600` throughout.
- Replace `font-weight:600` inline where the CSS class already provides it (reduce inline style duplication).
  </action>
  <verify>
Open dashboard in browser, expand "Detailed Analytics":
1. All section headers are flat (no colored bottom borders)
2. Other Metrics card shows Most/Least Profitable trips with dim backgrounds
3. Expense progress bars render correctly with CSS variable colors
4. Fuel Tracking shows both boxes with subtle left borders (2px, not 4px)
5. KPI metric cells use neutral backgrounds
6. Top Performers boxes use dim color backgrounds
7. Cost Trend sparklines render correctly
8. Trend badges show correct up/down/flat indicators
9. No hardcoded hex colors visible in devtools for any analytics element (except SVG sparkline stroke colors)
10. No font-weight above 600
11. Analytics section collapses/expands correctly
  </verify>
  <done>Entire analytics section restyled to flat treatment. All section headers use uniform section-header pattern. All hardcoded colors replaced with CSS variables. All font-weights capped at 600. Sparklines render correctly. Collapsible toggle works.</done>
</task>

</tasks>

<verification>
1. Full dashboard visual inspection -- consistent flat Stripe/Linear aesthetic from top to bottom
2. No hardcoded hex colors in any dashboard element (except SVG sparkline strokes)
3. No font-weight above 600 in any rendered element
4. No gradient backgrounds anywhere on dashboard
5. No colored section header borders (all use 1px var(--border))
6. Analytics section collapses and expands correctly
7. All onclick handlers still work (navigation, trip views, period selection)
8. Mobile responsive: sidebar stacks below main content on narrow screens
9. Right column cards have consistent neutral styling
</verification>

<success_criteria>
- Dashboard bottom half (sidebar + analytics) matches Stripe/Linear aesthetic
- All 3 success criteria from roadmap are met:
  1. KPI stat cards use label-above-number with flat backgrounds (Plan 01)
  2. Attention strip, main grid, sidebar use consistent neutral surfaces (Plan 01 + 02)
  3. Analytics section displays with clean flat styling (Plan 02)
- Zero hardcoded hex colors in dashboard render functions (except SVG)
- All font-weights capped at 600
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-restyle/20-02-SUMMARY.md`
</output>
