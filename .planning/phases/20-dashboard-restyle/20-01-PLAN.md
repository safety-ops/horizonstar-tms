---
phase: 20-dashboard-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/css/base.css
autonomous: true

must_haves:
  truths:
    - "KPI stat cards display label-above-number with flat backgrounds and subtle borders -- no icon boxes, gradient hero cards, or glow effects"
    - "Attention strip uses compact flat badges with system radius (6px/8px), not 20px rounded pills"
    - "Company Profitability section uses flat neutral card background, not dark gradient"
    - "All hardcoded hex colors in the top portion of renderDashboard are replaced with CSS variable references"
    - "Recent Trips section header includes 'View all' action link"
    - "Greeting header shows user's name and date"
  artifacts:
    - path: "assets/css/base.css"
      provides: "Dashboard-specific CSS classes (stat-card-label, stat-card-value, stat-card-sub, stat-card accent variants, section-header, attention-pill)"
      contains: ".stat-card-label"
    - path: "index.html"
      provides: "Restyled renderDashboard top half (header through profitability), cleaned inline style block"
  key_links:
    - from: "index.html renderDashboard"
      to: "assets/css/base.css"
      via: "CSS class references replacing inline styles"
      pattern: "stat-card-label|stat-card-value|section-header|attention-pill"
---

<objective>
Restyle the dashboard top section (page header, attention strip, KPI cards, recent trips, and company profitability) to the Stripe/Linear aesthetic.

Purpose: Transform the dashboard's most visible elements from the current gradient/glow/icon-box treatment to clean flat surfaces with label-above-number stat cards, compact attention badges, and neutral profitability section.
Output: Restyled CSS classes in base.css, cleaned inline style block, and rewritten renderDashboard template from page header through profitability section.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dashboard-restyle/20-RESEARCH.md
@.planning/phases/20-dashboard-restyle/20-CONTEXT.md
@assets/css/base.css
@assets/css/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard CSS classes and clean inline style block</name>
  <files>assets/css/base.css, index.html</files>
  <action>
**In base.css**, add these new dashboard-specific classes (after the existing `.stat-card` block around line 367):

1. `.stat-card-label` -- 11px, weight 500, text-muted, uppercase, letter-spacing 0.05em
2. `.stat-card-value` -- 20px, weight 600, font-mono, text-primary, letter-spacing -0.02em, tabular-nums
3. `.stat-card-sub` -- 11px, text-secondary
4. Accent variants: `.stat-card--green`, `.stat-card--red`, `.stat-card--blue`, `.stat-card--purple`, `.stat-card--amber`, `.stat-card--slate` -- each adds `border-left: 2px solid var(--green/red/blue/purple/amber/var(--text-muted))` respectively
5. `.section-header` -- flex, center aligned, space-between, padding 14px 16px, border-bottom 1px solid var(--border)
6. `.section-title` -- 14px, weight 600, text-primary, margin 0
7. `.section-link` -- 13px, weight 500, text-secondary, cursor pointer, no text-decoration; hover: text-primary
8. `.attention-pill` -- inline-flex, center aligned, gap 6px, padding 6px 12px, border-radius var(--radius-sm) (6px), border 1px solid, cursor pointer, font-size 12px
9. `.attention-pill-count` -- weight 600, font-size 13px
10. `.attention-pill-label` -- weight 500, font-size 12px
11. Attention pill color variants: `.attention-pill--amber` (background var(--amber-dim), border-color rgba amber 0.2, color var(--amber)), same pattern for `--green`, `--blue`, `--red`
12. `.profitability-cell` -- padding 16px 20px, text-align center, border-right 1px solid var(--border); last-child no border-right
13. `.dashboard-greeting` -- font-size 18px, weight 600, color text-primary, margin 0
14. `.dashboard-date` -- font-size 13px, color text-muted, margin-top 2px

**Update `.dashboard-kpi-grid`** in base.css (line 675): change `repeat(3, minmax(0, 1fr))` to `repeat(auto-fit, minmax(150px, 1fr))` to accommodate 5-6 cards.

**In the inline style block in index.html** (lines 1191-1360):
- Remove the `.stat-card:hover` rule (lines 1203-1206) that changes border to primary + shadow-md
- Remove `.stat-card .stat-icon` and all its color variants (lines 1207-1220) -- icon boxes are not used
- Remove `.stat-card .icon` and all its color variants (lines 1251-1264) -- duplicate icon boxes
- Remove the entire `.dashboard-stat-card` block and all its variants (lines 1267-1360) -- not used in any render function
- Update the `.stat-card` rule (lines 1191-1202) to match base.css pattern: `flex-direction: column` (not row), remove `align-items: flex-start`, change `border-radius` to `var(--radius)` (8px), change `background` to `var(--bg-card)`, change `box-shadow` to `var(--shadow-xs)`

Keep `.stat-card .label`, `.stat-card .value`, `.stat-card .trend` rules (lines 1224-1250) for now -- other pages may use them. But cap any font-weight values to 600 max.
  </action>
  <verify>
Open index.html in browser. Dashboard should still render (may look slightly different due to cascade changes). Verify:
- No CSS parse errors in browser devtools console
- `.stat-card` in devtools shows flex-direction: column
- `.dashboard-stat-card` class does NOT appear in Styles panel
- New classes (`.stat-card-label`, `.section-header`, `.attention-pill`) appear in base.css
  </verify>
  <done>base.css has all new dashboard CSS classes. Inline style block is cleaned of icon-box and premium card styles. stat-card renders vertically. No font-weight above 600 in any touched rule.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite renderDashboard top section (header through profitability)</name>
  <files>index.html</files>
  <action>
Rewrite the renderDashboard template literal from line 17388 (c.innerHTML assignment) through line 17501 (end of profitability IIFE). Keep all JavaScript computation logic above this point unchanged (lines 17188-17387 stay as-is).

**Page Header** (replace lines 17390-17399):
- Add greeting: `"Good morning, " + (currentUser?.first_name || currentUser?.name || 'there')` as `<div class="dashboard-greeting">`. Below it, date line: `new Date().toLocaleDateString('en-US', {weekday:'long', month:'long', day:'numeric', year:'numeric'})` in `<div class="dashboard-date">`.
- Keep period selector buttons but restyle: use `btn-secondary` for inactive, `btn-primary` for active period. Remove all inline style= from buttons, apply classes instead.
- Keep year/month selectors but use class="select" (from base.css component library) instead of inline styles.
- Keep Export and Refresh buttons as-is (already using btn classes).

**Attention Strip** (replace lines 17401-17432):
- Replace 4 inline-styled pill divs with `attention-pill` class + color variant.
- Remove all `onmouseover`/`onmouseout` translateY handlers.
- Pattern: `<div class="attention-pill attention-pill--amber" onclick="navigate('orders')"><span class="attention-pill-count">{count}</span><span class="attention-pill-label">Unassigned</span></div>`
- Remove icons from pills (just count + label).

**KPI Cards** (replace lines 17442-17447):
- Expand from 3 cards to 6 cards using the data already computed:
  1. Revenue (totalRev) -- sub: trips/cars -- accent: green
  2. Profit (totalProfit) -- sub: margin% -- accent: green or red conditional
  3. Expenses (totalBrokerFees+totalLocalFees+totalDriverCut+totalExpenses) -- sub: driver cuts amount -- accent: red
  4. Clean Gross (cleanGross) -- sub: "Rev - broker - local" -- accent: blue
  5. Miles (totalMiles) -- sub: source indicator -- accent: slate
  6. Avg/Car (avgPricePerCar) -- sub: cars count -- accent: purple
- Each card: `<div class="stat-card stat-card--{color}"><div class="stat-card-label">{label}</div><div class="stat-card-value">{value}</div><div class="stat-card-sub">{sublabel}</div></div>`
- For Profit card: stat-card-value gets `style="color: var(--green)"` or `style="color: var(--red)"` conditionally based on totalProfit >= 0.
- NO inline style= on cards except the conditional color on profit value.
- font-weight in stat-card-value must be 600 (from CSS class), never 700.

**Recent Trips section** (replace lines 17449-17454):
- Replace inline header with section-header pattern: `<div class="section-header"><h3 class="section-title">Recent Trips ({period})</h3><a class="section-link" onclick="navigate('trips')">View all &rarr;</a></div>`
- Remove the inline icon `[[icon:truck]]` from the title.
- Keep existing table/card rendering logic but fix hardcoded colors:
  - `color:#3b82f6` on trip numbers -> `color:var(--blue)`
  - `color:#047857` / `color:#dc2626` on mobile profit -> `color:var(--green)` / `color:var(--red)`
  - `font-weight:700` on any element -> remove (let CSS handle it)

**Company Profitability section** (replace lines 17456-17500):
- Remove `background:linear-gradient(135deg,#1e293b,#334155)` and `color:white` from the card wrapper. Use: `<div class="card card-flush" style="margin-bottom:var(--space-5)">`.
- Replace header with section-header pattern: `<div class="section-header"><h3 class="section-title">Company Profitability</h3>` + miles/cars badge on right.
- Warning banner: replace `background:rgba(251,191,36,0.2)` with `background:var(--amber-dim)`. Replace hardcoded colors with var references.
- Profitability grid cells: use `.profitability-cell` class instead of inline padding/border styles.
- Replace all hardcoded hex colors in this section:
  - `#94a3b8` -> `var(--text-muted)`
  - `#f87171` -> `var(--red)`
  - `#4ade80` -> `var(--green)`
  - `#60a5fa` -> `var(--blue)`
  - `#a78bfa` -> `var(--purple)`
  - `#86efac` / `#fca5a5` -> `var(--green)` / `var(--red)`
  - `rgba(255,255,255,0.1)` borders -> `var(--border)`
  - `rgba(34,197,94,0.15)` backgrounds -> `var(--green-dim, rgba(34,197,94,0.08))`
  - `rgba(239,68,68,0.15)` backgrounds -> `var(--red-dim, rgba(239,68,68,0.08))`
- Replace `font-size:28px;font-weight:700` with `font-size:var(--text-xl);font-weight:600;font-family:var(--font-mono)` on metric values.
- Remove `font-weight:700` everywhere in this section -- use 600 max.
- Remove white-on-dark color assumptions (all text should use `var(--text-primary)`, `var(--text-muted)`, `var(--text-secondary)` etc.).

**Keep the dash-main-grid injected style** (line 17386) but change gap from `20px` to `var(--space-5)` and sidebar gap from `16px` to `var(--space-4)`.
  </action>
  <verify>
Open index.html in browser, navigate to Dashboard:
1. Greeting header shows "Good morning, {name}" with today's date
2. Attention strip shows 4 compact flat badges (no large pills, no translateY hover)
3. KPI row shows 6 stat cards in horizontal strip with label above number
4. Recent Trips has "View all" link in section header
5. Company Profitability section has flat white/neutral background (no dark gradient)
6. No hardcoded hex colors visible in devtools for elements in top section
7. No font-weight above 600 in any rendered element
8. Mobile responsive: KPI cards wrap on narrow screens
  </verify>
  <done>Dashboard top section (header, attention strip, KPI cards, recent trips, profitability) fully restyled to Stripe/Linear aesthetic. All inline styles replaced with CSS classes. No gradients, no icon boxes, no glow effects, no font-weight 700.</done>
</task>

</tasks>

<verification>
1. Open dashboard in browser -- visual inspection confirms flat, clean aesthetic
2. Browser devtools: no hardcoded hex colors in top section elements
3. Browser devtools: no font-weight above 600
4. Browser devtools: stat-card shows flex-direction: column (not row with icon)
5. Mobile viewport (375px): KPI cards stack, attention pills wrap, profitability grid collapses
6. Period selector buttons work (All Time, Year, Month, Q1-Q4)
7. "View all" link on Recent Trips navigates to trips page
</verification>

<success_criteria>
- Dashboard top half matches Stripe/Linear aesthetic: flat surfaces, label-above-number stat cards, compact attention badges, no gradients
- 6 KPI cards visible with correct data (revenue, profit, expenses, clean gross, miles, avg/car)
- Company Profitability section has neutral card background instead of dark gradient
- All colors use CSS variables, all weights capped at 600
- No visual regressions on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-restyle/20-01-SUMMARY.md`
</output>
