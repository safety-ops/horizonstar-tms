---
phase: 25-operations-admin-restyle
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [index.html, assets/css/base.css]
autonomous: true

must_haves:
  truths:
    - "Chat presence dots use CSS variables (var(--green), var(--amber), var(--text-muted)) instead of hardcoded hex"
    - "Chat mention chips use var(--amber-dim) and var(--amber) instead of hardcoded rgba and hex"
    - "Chat file icons use var(--blue-dim) instead of hardcoded rgba"
    - "Chat input area has pill-style rounded treatment"
    - "No glow effects on any chat elements"
  artifacts:
    - path: "index.html"
      provides: "Chat CSS block with CSS variable colors"
      contains: "var\\(--green\\)|var\\(--amber\\)|var\\(--text-muted\\)"
    - path: "assets/css/base.css"
      provides: "Entity card presence dots with CSS variables"
      contains: "var\\(--green\\)|var\\(--amber\\)"
  key_links:
    - from: "Chat CSS block (lines 3943-4600)"
      to: "variables.css"
      via: "CSS custom properties"
      pattern: "var\\(--green\\)|var\\(--amber\\)|var\\(--blue-dim\\)"
---

<objective>
Restyle Team Chat CSS to use CSS variables instead of hardcoded hex colors, and add pill-style chat input treatment.

Purpose: The chat layout is already Slack-like and well-structured. This plan does targeted CSS variable swaps (~10 changes) and a pill-style input treatment -- no structural changes.
Output: Chat with CSS variable presence dots, mention chips, file icons, and rounded pill input.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operations-admin-restyle/25-CONTEXT.md
@.planning/phases/25-operations-admin-restyle/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat CSS variable swaps + pill input</name>
  <files>index.html, assets/css/base.css</files>
  <action>
**Chat CSS block in index.html** (around lines 3943-4600):

1. **Presence indicators**:
   - `.chat-status-ping`: Replace `#22c55e` with `var(--green)`
   - `.chat-presence.online`: Replace `#22c55e` with `var(--green)`
   - `.chat-presence.away`: Replace `#f59e0b` with `var(--amber)`
   - `.chat-presence.offline`: Replace `#71717a` with `var(--text-muted)`

2. **Mention chips**:
   - `.chat-mention-chip`: Replace `rgba(245, 197, 66, 0.18)` background with `var(--amber-dim)`, replace `#92640d` text color with `var(--amber)`

3. **File icons**:
   - `.chat-file-icon`: Replace `rgba(99, 102, 241, 0.10)` with `var(--blue-dim)`

4. **Error state**:
   - `.chat-state.error .chat-state-title`: Replace `#dc2626` with `var(--red)`

5. **Pill-style chat input**: Update the chat composer/input area CSS:
   - Add `border-radius: 24px` to the chat input wrapper/shell (the container around the text input + send button)
   - Ensure the text input inside has no visible border of its own (border handled by wrapper)
   - The send button should sit flush inside the pill shape
   - Use `background: var(--bg-card)` and `border: 1px solid var(--border)` on the wrapper
   - Do NOT apply the standard `.input` class -- this gets chat-specific pill treatment per CONTEXT.md

6. **Glow removal**: Search for any `box-shadow` with colored values (not `var(--shadow-*)`) and replace with `var(--shadow-xs)` or remove entirely.

7. **Message bubble colors**: If sent messages use gradient backgrounds, replace with `background: var(--bg-tertiary)`. If received messages use gradient backgrounds, replace with `background: var(--bg-card)`. Per CONTEXT.md: keep distinct bubble shapes, flatten colors.

**base.css entity cards** (around lines 1667-1726):

8. **Presence dots in entity cards**:
   - Replace `#22c55e` (online) with `var(--green)`
   - Replace `#f59e0b` (away) with `var(--amber)`
   - Replace `#71717a` (offline) with `var(--text-muted)`

Commit: `feat(25-04): restyle team chat`

Do NOT change chat message rendering logic, realtime subscription, or presence calculation. Do NOT restructure the chat layout -- it's already Slack-like per CONTEXT.md/RESEARCH.md.
  </action>
  <verify>Search the chat CSS block (lines 3943-4600) and base.css entity card section (lines 1667-1726) for hardcoded hex: #22c55e, #f59e0b, #71717a, #dc2626, #92640d, rgba(245, rgba(99. All should return zero matches in these sections. Verify border-radius:24px exists on chat composer.</verify>
  <done>Chat presence dots, mention chips, file icons, and error states all use CSS variables. Pill-style input with 24px border-radius. No glow effects. Entity card presence dots use CSS variables. Zero hardcoded hex in chat CSS and entity card sections.</done>
</task>

</tasks>

<verification>
1. Open the app, navigate to Team Chat -- message bubbles should display with flat colors (no gradients)
2. Check presence dots next to usernames -- should still show green/amber/gray colors (now via CSS variables)
3. Chat input area should appear as a rounded pill shape
4. If any @mention chips visible, they should use amber tint background
5. Check entity link cards in chat messages -- presence dots should match
</verification>

<success_criteria>
- All hardcoded hex in chat CSS block replaced with CSS variables
- All hardcoded hex in base.css entity card presence dots replaced with CSS variables
- Chat input has pill-style rounded treatment (border-radius: 24px)
- No glow effects (box-shadow with color values) on chat elements
- Message bubbles use CSS variable backgrounds, not gradients
- Single commit
</success_criteria>

<output>
After completion, create `.planning/phases/25-operations-admin-restyle/25-04-SUMMARY.md`
</output>
