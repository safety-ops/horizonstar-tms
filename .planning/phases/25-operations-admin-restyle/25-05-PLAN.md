---
phase: 25-operations-admin-restyle
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Executive dashboard health score uses stat-flat pattern -- no gradient banner, no 48px font, no font-weight:800"
    - "All executive dashboard card-headers use section-header pattern -- no linear-gradient backgrounds, no white text"
    - "KPI stat cards use stat-flat + stat-card--{color} accent borders, not hardcoded hex border-left"
    - "Dark profitability panel replaced with flat card + section-header -- no dark gradient background, no white text"
    - "P&L table rows use CSS variable dim colors (green-dim, red-dim, amber-dim) for semantic backgrounds"
    - "Bar charts use solid CSS variable colors instead of linear-gradient fills"
    - "All font-weight:800 instances replaced with 600"
    - "Rankings/leaderboard tables use data-table class"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderExecutiveDashboard"
      contains: "stat-flat|section-header-title|data-table|var\\(--green-dim\\)|var\\(--red-dim\\)"
  key_links:
    - from: "renderExecutiveDashboard"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|section-header|data-table"
    - from: "renderExecutiveDashboard P&L tables"
      to: "variables.css"
      via: "CSS custom properties"
      pattern: "var\\(--green-dim\\)|var\\(--red-dim\\)|var\\(--amber-dim\\)"
---

<objective>
Restyle the Executive Dashboard (renderExecutiveDashboard) -- the most anti-pattern-dense page in the app -- to the Stripe/Linear aesthetic.

Purpose: The executive dashboard has ~50-60 anti-patterns including gradient banners, dark panels, 800-weight fonts, and hardcoded hex throughout. This plan systematically flattens everything to match the main dashboard (Phase 20) treatment. No special executive treatment per CONTEXT.md.
Output: Executive dashboard with flat stat cards, section headers, CSS variable colors, data-tables, and 600-weight typography throughout.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operations-admin-restyle/25-CONTEXT.md
@.planning/phases/25-operations-admin-restyle/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten executive dashboard top sections (health score, KPIs, profitability panel, income/cash flow)</name>
  <files>index.html</files>
  <action>
**renderExecutiveDashboard** (around line 12662-13169). Work top-to-bottom through the first ~250 lines of the function:

1. **Health Score Banner** (around lines 12744-12771):
   - Remove the gradient banner entirely: `linear-gradient(135deg,${healthColor}20,${healthColor}10)`, the `border:2px solid ${healthColor}`, the `font-size:48px`, and `font-weight:800`
   - Replace with a `stat-flat` + `stat-card--{color}` card (color based on health grade: green for A/B, amber for C, red for D/F):
     ```javascript
     '<div class="stat-flat stat-card--' + healthAccentColor + '">' +
     '<div class="stat-flat-label">Company Health Score</div>' +
     '<div class="stat-flat-value" style="font-family:var(--font-mono)">' + healthGrade + ' (' + healthScore + '%)</div>' +
     '</div>'
     ```
   - The health color variable used for the accent should map: green grades -> 'green', amber grades -> 'amber', red grades -> 'red'

2. **AI Recommendations card-header** (around line 12775):
   - Replace `background:linear-gradient(135deg,#f59e0b,#fbbf24);color:white` with section-header pattern:
     `<div class="section-header"><span class="section-header-title">AI Recommendations</span></div>`

3. **KPI Stat Cards** (around lines 12784-12817, 4 cards):
   - Replace `border-left:4px solid #22c55e` (and similar) with `stat-flat stat-card--green` (and appropriate colors)
   - Replace `.stat-icon` with hardcoded backgrounds -> remove stat-icon entirely
   - Apply `stat-flat-label`, `stat-flat-value` pattern
   - Values get `font-family:var(--font-mono)`

4. **Quick View Dark Panel** (around lines 12835-12872):
   - Replace `background:linear-gradient(135deg,#1e293b,#334155);color:white` with flat card:
     `<div class="card card-flush">`
   - Add section-header: `<div class="section-header"><span class="section-header-title">Quick View - Company Profitability</span></div>`
   - Replace `rgba(255,255,255,0.1)` borders with `var(--border)`
   - Replace `#94a3b8` -> `var(--text-secondary)`, `#f87171` -> `var(--red)`, `#4ade80` -> `var(--green)`
   - Replace `font-weight:700/800` -> `font-weight:600`
   - All text becomes `color:var(--text-primary)` or `var(--text-secondary)` (no white text on dark bg)

5. **Income Statement card-header** (around line 12880):
   - Replace `background:linear-gradient(135deg,#059669,#10b981);color:white` with section-header pattern

6. **P&L table rows** (around lines 12884-12903):
   - Replace hardcoded hex backgrounds with CSS variable dim equivalents:
     - `#f0fdf4` / `#dcfce7` (green tints) -> `var(--green-dim)` for revenue rows
     - `#fef2f2` / `#fee2e2` (red tints) -> `var(--red-dim)` for expense/cost rows
     - `#bbf7d0` (bold green) -> `var(--green-dim)` for net income row
   - Keep the semantic meaning (green = revenue, red = costs) but use CSS variables
   - Add `class="data-table"` to the P&L table

7. **Cash Flow card-header** (around line 12910):
   - Replace `background:linear-gradient(135deg,#1e40af,#3b82f6);color:white` with section-header pattern

8. **All font-weight:800 in this section** -> `font-weight:600`
9. **All font-weight:700 in this section** -> `font-weight:600`

Do NOT touch the tab bar at lines 12712-12731 (owned by Phase 24 financials). Do NOT change any calculation, scoring, or data logic.
  </action>
  <verify>Search lines 12662-12900 of renderExecutiveDashboard for: linear-gradient, font-weight:800, font-weight:700, #1e293b, #334155, rgba(255, #059669, #10b981, #1e40af, #3b82f6, #f0fdf4, #fef2f2, #bbf7d0, #dcfce7, #fee2e2. All should return zero matches. Confirm stat-flat and section-header-title classes present.</verify>
  <done>Health score is a flat stat-flat card. AI recommendations, income statement, cash flow all use section-header. KPIs use stat-flat + accent borders. Dark panel replaced with flat card. P&L uses CSS variable dim colors. All font-weights at 600.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten executive dashboard bottom sections (trip volume, forecast, break-even, scenarios, truck performance)</name>
  <files>index.html</files>
  <action>
Continue in **renderExecutiveDashboard** from approximately line 12900 to 13169:

1. **Trip Volume card-header** (around line 12939):
   - Replace `background:linear-gradient(135deg,#7c3aed,#a855f7);color:white` with section-header pattern

2. **Monthly bar charts** (around line 12982):
   - Replace `linear-gradient(90deg,#22c55e,#16a34a)` with solid `background:var(--green)`
   - Replace `linear-gradient(90deg,#f59e0b,#d97706)` with solid `background:var(--amber)`
   - Keep the bar chart structure intact -- just flatten gradient fills to solid CSS variable colors

3. **Truck Performance table** (around line 13018):
   - Add `class="data-table"` to the table
   - Replace inline hex color map for status badges with badge classes (`badge-green`, `badge-amber`, `badge-red`, `badge-gray`)

4. **Forecast card-header** (around line 13030):
   - Replace `background:linear-gradient(135deg,#0891b2,#06b6d4);color:white` with section-header pattern

5. **Forecast table/data**: Add `class="data-table"` if applicable. Replace any hardcoded hex with CSS variables.

6. **Scenario cards** (around lines 13088-13102):
   - Replace hardcoded hex backgrounds with CSS variable dim equivalents:
     - Green scenario -> `background:var(--green-dim)`
     - Amber/yellow scenario -> `background:var(--amber-dim)`
     - Red scenario -> `background:var(--red-dim)`
   - Text colors: `var(--text-primary)` (not white)

7. **Break-Even card-header** (around line 13110):
   - Replace `background:linear-gradient(135deg,#dc2626,#ef4444);color:white` with section-header pattern

8. **Break-Even stats** (around lines 13115-13134):
   - Replace hardcoded hex borders and backgrounds with CSS variable equivalents
   - Apply `stat-flat` pattern if applicable, or at minimum replace colors

9. **Final sweep of entire function**:
   - Search for any remaining hardcoded hex (#xxxxxx pattern) and replace with CSS variables
   - Search for any remaining font-weight:700 or font-weight:800 and cap at 600
   - Search for any remaining linear-gradient and remove
   - Search for any remaining rgba() and replace with CSS variables where possible

Commit: `feat(25-05): restyle executive dashboard`

Do NOT change any calculation, scoring, chart rendering logic, or data fetching.
  </action>
  <verify>Search the ENTIRE renderExecutiveDashboard function (lines 12662-13169) for: linear-gradient, font-weight:700, font-weight:800, rgba(255. ALL should return zero. Count remaining hardcoded hex (#) occurrences -- should be zero or near-zero (only acceptable if inside data values, not styling). Confirm section-header-title appears for every card section. Confirm data-table on truck performance and forecast tables.</verify>
  <done>All executive dashboard card-headers use section-header (no gradients, no white text). Bar charts use solid CSS variable colors. Truck performance and forecast use data-table. Scenario cards use dim CSS variable backgrounds. Break-even uses flat styling. Zero gradients, zero font-weight above 600, zero hardcoded hex in styling across entire function.</done>
</task>

</tasks>

<verification>
1. Open the app, navigate to Financials -> Executive Dashboard tab
2. Health score should display as a flat stat card with colored accent border (not a large gradient banner)
3. All section headers should be flat 14px/600 text (no gradient colored headers)
4. Quick View profitability should be a flat card on light background (no dark gradient panel)
5. P&L tables should show green-dim for revenue rows, red-dim for expense rows (semantic colors preserved)
6. Bar charts should use solid green and amber fills (no gradient bars)
7. Scenario cards should use dim background tints (green-dim, amber-dim, red-dim)
8. Truck performance table should have alternating row tint and badge-class status indicators
9. No white-on-dark text anywhere -- all text should be text-primary/secondary on light surfaces
</verification>

<success_criteria>
- Zero linear-gradient references in renderExecutiveDashboard
- Zero font-weight above 600 in renderExecutiveDashboard
- Zero hardcoded hex colors used for styling in renderExecutiveDashboard
- All card-headers replaced with section-header pattern
- Health score uses stat-flat (no special treatment)
- Dark profitability panel replaced with flat card
- P&L semantic colors preserved via CSS variable dim equivalents
- Bar charts use solid CSS variable colors
- All tables use data-table class
- Single commit for entire executive dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/25-operations-admin-restyle/25-05-SUMMARY.md`
</output>
