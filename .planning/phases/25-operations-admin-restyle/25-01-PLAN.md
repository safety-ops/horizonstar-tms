---
phase: 25-operations-admin-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Activity log stat cards display as flat stat-flat components with accent borders, not gradient backgrounds"
    - "Activity log action badges use badge-blue/amber/red/green/gray classes instead of inline hex color styling"
    - "Activity log table uses data-table class with alternating row tint"
    - "Maintenance stat card uses stat-card--green accent border instead of hardcoded #10b981"
    - "Maintenance table uses data-table class"
    - "Maintenance filter selects use .select component class"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderActivityLog, showActivityDetail, renderMaintenance"
      contains: "stat-flat|data-table|badge-blue|badge-amber"
  key_links:
    - from: "renderActivityLog"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|data-table|badge-"
    - from: "renderMaintenance"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|stat-card--green|data-table"
---

<objective>
Restyle the Activity Log page (renderActivityLog, showActivityDetail) and the Maintenance page (renderMaintenance) to the Stripe/Linear aesthetic.

Purpose: Knocks out two simpler operations pages, establishing the pattern for admin page restyles.
Output: Activity log with flat stat cards, categorized badge-class action labels, and data-table. Maintenance with flat stat card, data-table, and component-class filters.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operations-admin-restyle/25-CONTEXT.md
@.planning/phases/25-operations-admin-restyle/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle renderActivityLog and showActivityDetail</name>
  <files>index.html</files>
  <action>
**renderActivityLog** (around line 45514-45653):

1. **Stat cards**: Replace gradient stat cards (linear-gradient backgrounds) with `.stat-flat` class pattern:
   - `<div class="stat-flat stat-card--blue">` (or --green/--amber/--red as appropriate for each stat)
   - `<div class="stat-flat-label">Label</div>`
   - `<div class="stat-flat-value" style="font-family:var(--font-mono)">Value</div>`
   Remove any `linear-gradient()`, `rgba()` backgrounds, or inline color styling on stat cards.

2. **Action badge colors (actionColors map)**: Replace the `actionColors` object that maps ~20 action types to specific hex colors. Instead, create a function or mapping that groups actions into 5 badge categories:
   - `badge-blue`: created, added, imported, enabled, assigned
   - `badge-amber`: updated, edited, modified, changed, moved
   - `badge-red`: deleted, removed, disabled, cancelled
   - `badge-green`: completed, approved, paid, delivered, resolved
   - `badge-gray`: login, logout, viewed, exported, other/default
   Replace the inline badge styling (`background:${color}22;color:${color}` pattern) with the appropriate badge class: `<span class="badge ${badgeClass}">${action}</span>`.

3. **Table**: Add `class="data-table"` to the main activity log table. Wrap table in `card-flush` if not already wrapped.

4. **Filter area**: Apply `.select` class to filter dropdown(s). Apply `.input` class to any date inputs. Remove hardcoded border colors on filter elements.

5. **Font weights**: Cap all `font-weight:700` or `font-weight:800` to `font-weight:600`.

6. **Hardcoded colors**: Replace any remaining hardcoded hex colors with CSS variable equivalents (`var(--text-primary)`, `var(--text-secondary)`, `var(--border)`, etc.).

**showActivityDetail** (around line 45656-45683): Replace any hardcoded inline styles with CSS variable equivalents. Minor cleanup only.

Commit this page separately: `feat(25-01): restyle activity log page`

Do NOT change any data fetching, filtering logic, or export functionality.
  </action>
  <verify>Search renderActivityLog for: linear-gradient, font-weight:700, font-weight:800, actionColors (as hex map). All gradient/weight matches should be zero. Confirm stat-flat, data-table, and badge- classes are present. Confirm actionColors replaced with badge-class mapping.</verify>
  <done>Activity log uses stat-flat cards with accent borders, badge-class action labels (5 categories), data-table with alternating rows, .select/.input on filters. Zero hardcoded hex, zero gradients, zero font-weight above 600.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle renderMaintenance</name>
  <files>index.html</files>
  <action>
**renderMaintenance** (around line 39817-39892):

1. **Stat card**: Replace the stat card with hardcoded `#10b981` border with `.stat-flat stat-card--green` pattern:
   - `<div class="stat-flat stat-card--green">`
   - `<div class="stat-flat-label">Label</div>`
   - `<div class="stat-flat-value" style="font-family:var(--font-mono)">Value</div>`
   If there are multiple stat cards, apply appropriate `stat-card--{color}` accents.

2. **Table**: Add `class="data-table"` to the maintenance records table. Wrap in `card-flush` if not already.

3. **Filters**: Apply `.select` class to all filter dropdowns. Remove inline filter styles (hardcoded padding, border, etc.) -- the `.select` class handles this.

4. **Page header**: Set heading to `font-size:18px;font-weight:600` if not already.

5. **Hardcoded colors**: Replace `#10b981` and any other hardcoded hex with CSS variable equivalents.

6. **Preserve responsive classes**: The maintenance page already uses `responsive-*` CSS classes -- keep these intact.

Commit this page separately: `feat(25-01): restyle maintenance page`

Do NOT change data fetching, sorting, or any maintenance record logic.
  </action>
  <verify>Search renderMaintenance for: #10b981, font-weight:700, font-weight:800, linear-gradient. All should return zero matches. Confirm stat-flat, stat-card--green, data-table, and .select classes are present.</verify>
  <done>Maintenance page uses stat-flat cards with green accent, data-table with alternating rows, .select on filters. Zero hardcoded hex, zero gradients, 18px/600 header.</done>
</task>

</tasks>

<verification>
1. Open the app, navigate to Activity Log -- stat cards should be flat with colored left borders, action labels should show as colored badges (blue/amber/red/green/gray), table should have alternating row tint
2. Click an activity row detail -- modal should use clean CSS variable styling
3. Navigate to Maintenance -- stat card should have green left border accent, table should have alternating rows, filters should use standard select styling
4. Verify no visual regressions in either page's data display or filtering
</verification>

<success_criteria>
- renderActivityLog uses stat-flat cards, badge-class action labels, data-table
- renderMaintenance uses stat-flat card with green accent, data-table, .select filters
- Zero hardcoded hex colors in both functions
- Zero font-weight above 600 in both functions
- Zero linear-gradient references in both functions
- Two separate commits (one per page)
</success_criteria>

<output>
After completion, create `.planning/phases/25-operations-admin-restyle/25-01-SUMMARY.md`
</output>
