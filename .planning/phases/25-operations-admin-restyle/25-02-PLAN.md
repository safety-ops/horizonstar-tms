---
phase: 25-operations-admin-restyle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Task stat cards display as flat stat-flat components, not colored background cards"
    - "Task filter buttons use segmented-control instead of btn-primary/btn-secondary toggles"
    - "Task items use CSS variable accent borders instead of hardcoded #e5e7eb border-left"
    - "Task modal form elements use .input and .select component classes"
    - "Settings form inputs use .input component class throughout"
    - "Settings status indicators use CSS variables instead of hardcoded #059669, #dc2626, #1d4ed8"
    - "Settings card headers use section-header pattern"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderTasks, openTaskModal, renderSettings"
      contains: "stat-flat|segmented-control|section-header-title"
  key_links:
    - from: "renderTasks"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|segmented-control-btn"
    - from: "renderSettings"
      to: "base.css"
      via: "CSS component classes"
      pattern: "input|section-header-title"
---

<objective>
Restyle the Tasks page (renderTasks, openTaskModal) and the Settings page (renderSettings) to the Stripe/Linear aesthetic.

Purpose: Completes the simpler admin pages, applying flat stat cards, segmented controls, and form input standardization.
Output: Tasks with flat stat cards, segmented-control filter, clean task items. Settings with standardized inputs, section headers, CSS variable status indicators.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operations-admin-restyle/25-CONTEXT.md
@.planning/phases/25-operations-admin-restyle/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle renderTasks and openTaskModal</name>
  <files>index.html</files>
  <action>
**renderTasks** (around line 26795-26896):

1. **Stat cards**: Convert existing stat-card elements (which use colored backgrounds like `var(--red-dim)`) to `.stat-flat` + `.stat-card--{color}` pattern:
   - `<div class="stat-flat stat-card--blue">` (total), `stat-card--amber` (pending), `stat-card--red` (overdue), `stat-card--green` (completed) -- adjust colors to match semantic meaning
   - `<div class="stat-flat-label">Label</div>`
   - `<div class="stat-flat-value" style="font-family:var(--font-mono)">Count</div>`

2. **Filter button row**: Convert filter buttons (All/Pending/In Progress/Completed/Overdue) from btn-primary/btn-secondary toggles to segmented-control:
   ```javascript
   '<div class="segmented-control">' +
   filters.map(f => '<button class="segmented-control-btn' + (isActive ? ' active' : '') + '" onclick="...">' + f.label + '</button>').join('') +
   '</div>'
   ```
   Preserve the filter onclick logic exactly.

3. **Task items**: Replace inline `border-left:4px solid #e5e7eb` with `border-left:3px solid var(--border)`. For overdue tasks, use `border-left:3px solid var(--red)`. For urgent tasks, use `border-left:3px solid var(--amber)`. Remove any hardcoded background colors on task cards.

4. **Page header**: Set to `font-size:18px;font-weight:600`.

5. **Font weights**: Cap all at 600.

6. **Hardcoded colors**: Replace `#e5e7eb` -> `var(--border)`, and any other hardcoded hex with CSS variables.

**openTaskModal** (around line 26898-26912):

1. Apply `.input` class to all text/textarea inputs in the modal form.
2. Apply `.select` class to all select elements (priority, status, assignee dropdowns).
3. Replace any hardcoded inline styles on form elements.

Commit: `feat(25-02): restyle tasks page`

Do NOT change task CRUD logic, filtering behavior, or modal submission handlers.
  </action>
  <verify>Search renderTasks for: #e5e7eb, font-weight:700, font-weight:800, linear-gradient. All should return zero. Confirm stat-flat, segmented-control-btn classes present. Search openTaskModal for form elements -- confirm .input and .select classes applied.</verify>
  <done>Tasks page uses stat-flat cards with accent borders, segmented-control filter, CSS variable task item borders, .input/.select in modal. Zero hardcoded hex, zero font-weight above 600.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle renderSettings</name>
  <files>index.html</files>
  <action>
**renderSettings** (around line 40111-40279):

1. **Form inputs**: Apply `class="input"` to ALL `<input>` elements (text, password, email, number types). Apply `class="select"` to any `<select>` elements. Apply `class="textarea"` to any `<textarea>` elements. Remove inline width/padding/border styles that the component classes already handle.

2. **Card headers**: Replace `<div class="card-header"><h3>Title</h3></div>` with the section-header pattern:
   ```javascript
   '<div class="section-header"><span class="section-header-title">Title</span></div>'
   ```
   Apply this to all 6 settings card sections.

3. **Status indicators**: Replace hardcoded hex colors in status messages:
   - `#059669` (green success) -> `var(--green)`
   - `#dc2626` (red error) -> `var(--red)`
   - `#1d4ed8` (blue info) -> `var(--blue)`
   - `#047857` (dark green) -> `var(--green)`
   These appear in inline style on status text spans. Convert to use CSS variables.

4. **Status backgrounds**: Verify `var(--dim-green)`, `var(--dim-red)`, `var(--dim-amber)` references. The correct naming convention is `var(--green-dim)`, `var(--red-dim)`, `var(--amber-dim)`. Fix if using wrong prefix.

5. **Card wrapper**: Apply `card-flush` class to settings card containers. Keep the `max-width:700px` constraint (settings forms benefit from constrained width -- this is intentional UX, not an anti-pattern).

6. **Page header**: Set to `font-size:18px;font-weight:600`.

7. **Button styling**: Ensure save/update buttons use `btn-primary` class. Ensure any secondary actions use `btn-secondary` or `btn-ghost`.

8. **Font weights**: Cap all at 600.

Commit: `feat(25-02): restyle settings page`

Do NOT change any settings save logic, validation, or API calls.
  </action>
  <verify>Search renderSettings for: #059669, #dc2626, #1d4ed8, #047857, font-weight:700, font-weight:800, card-header. All hardcoded hex and weight matches should be zero. Confirm .input class on form inputs, section-header-title on card headers, btn-primary on save buttons.</verify>
  <done>Settings page uses .input/.select on all form elements, section-header pattern on card headers, CSS variable status colors, btn-primary/secondary buttons. Zero hardcoded hex, zero font-weight above 600.</done>
</task>

</tasks>

<verification>
1. Open the app, navigate to Tasks -- stat cards should be flat with accent borders, filter row should be a segmented control, task items should have clean borders
2. Click "New Task" -- modal form should have styled inputs and selects
3. Navigate to Settings -- all 6 card sections should have flat headers (14px/600), form inputs should have consistent height and borders, save buttons should be dark slate primary
4. Trigger a settings save -- status messages should display with CSS variable colors (green for success, red for error)
</verification>

<success_criteria>
- renderTasks uses stat-flat cards, segmented-control filter, CSS variable borders on task items
- openTaskModal uses .input/.select on all form elements
- renderSettings uses .input on all inputs, section-header on cards, CSS variable status colors
- Zero hardcoded hex colors in all three functions
- Zero font-weight above 600
- Two separate commits (one per page)
</success_criteria>

<output>
After completion, create `.planning/phases/25-operations-admin-restyle/25-02-SUMMARY.md`
</output>
