---
phase: 25-operations-admin-restyle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Compliance 7-tab bar uses segmented-control-scroll component, not inline-styled buttons"
    - "Compliance dashboard hero card is flattened -- no blue background, no white text, stat items use stat-flat pattern"
    - "All compliance tables (tasks, driver files, truck files, company files) use data-table class"
    - "Expiration warnings use accent border (red for expired, amber for warning) plus badge-red/badge-amber status badges"
    - "Company files folder items use var(--bg-secondary) instead of var(--bg-darker)"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderCompliance, renderComplianceDashboard, renderComplianceTasks, renderComplianceDriverFiles, renderComplianceTruckFiles, renderComplianceCompanyFiles, renderComplianceDriverSection"
      contains: "segmented-control-scroll|stat-flat|data-table|badge-red|badge-amber"
  key_links:
    - from: "renderCompliance"
      to: "base.css"
      via: "CSS component classes"
      pattern: "segmented-control-scroll"
    - from: "renderComplianceDashboard"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|stat-card--"
    - from: "renderComplianceDriverFiles"
      to: "base.css"
      via: "CSS component classes"
      pattern: "data-table|badge-red|badge-amber"
---

<objective>
Restyle the entire Compliance section (7 tabs, dashboard hero, file tables, expiration warnings) to the Stripe/Linear aesthetic.

Purpose: The compliance section is the most complex operations page with 500+ lines across 7+ sub-functions. This plan converts the blue hero dashboard to flat stat cards, standardizes all tables, and implements the accent-border + badge pattern for expiration urgency.
Output: Compliance with segmented-control-scroll tabs, flat dashboard, data-table on all tables, accent border expiration warnings.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operations-admin-restyle/25-CONTEXT.md
@.planning/phases/25-operations-admin-restyle/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle compliance shell + dashboard hero</name>
  <files>index.html</files>
  <action>
**renderCompliance** (around line 26975-26994):

1. **7-tab bar**: Replace inline-styled tab buttons (which use per-tab `color` with inline background) with `segmented-control-scroll`:
   ```javascript
   '<div class="segmented-control-scroll">' +
   tabs.map(t => '<button class="segmented-control-btn' + (isActive ? ' active' : '') + '" onclick="...">' + t.label + '</button>').join('') +
   '</div>'
   ```
   Remove all per-tab color differentiation -- active state comes from the component class. Preserve onclick handlers and tab switching logic exactly.

2. **Page header**: Set to `font-size:18px;font-weight:600`.

**renderComplianceDashboard** (around line 27007-27159):

1. **Hero card**: The blue hero card with `background:var(--blue)` and white text must be completely flattened. Replace with a grid of `stat-flat` cards:
   - Remove the blue background, white text, and the entire hero card wrapper
   - Convert each stat item (which uses `rgba(255,255,255,0.15)` backgrounds) into individual `stat-flat` + `stat-card--{color}` cards
   - Arrange in a responsive grid: `display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px`

2. **Card headers (h4)**: Replace any `<div class="card-header"><h4>Title</h4></div>` with the section-header pattern:
   ```javascript
   '<div class="section-header"><span class="section-header-title">Title</span></div>'
   ```

3. **Summary items/lists**: If there are `var(--bg-darker)` items, replace with `var(--bg-secondary)`.

4. **Font weights**: Cap all at 600.

5. **Hardcoded colors**: Replace all hardcoded hex and `rgba()` values with CSS variable equivalents.

Do NOT change any compliance scoring logic or data calculations.
  </action>
  <verify>Search renderCompliance and renderComplianceDashboard for: linear-gradient, rgba(255, font-weight:700, font-weight:800, bg-darker. All should return zero. Confirm segmented-control-scroll on tab bar, stat-flat on dashboard stats.</verify>
  <done>Compliance tabs use segmented-control-scroll (7 tabs). Dashboard hero flattened to stat-flat grid. No blue backgrounds, no white text, no rgba overlays. All card headers use section-header pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle compliance sub-tabs (files, tasks, tickets/violations)</name>
  <files>index.html</files>
  <action>
**renderComplianceTasks** (around line 27162-27231):

1. **IFTA banner**: If using `var(--blue-dim)` background, keep it but ensure it uses the accent-border pattern: `border-left:3px solid var(--blue)` + `background:var(--blue-dim)`.
2. **Table**: Add `class="data-table"` to the compliance tasks table.
3. **Expiration badges**: Apply `badge-red` for expired items, `badge-amber` for items expiring soon.

**renderComplianceDriverFiles** (around line 27314-27360):

1. **Alert/warning section**: Replace inline colored alert with accent-border pattern: `border-left:3px solid var(--red)` for expired, `border-left:3px solid var(--amber)` for warning. Use `badge-red` and `badge-amber` for status text.
2. **Table**: Add `class="data-table"`. Wrap in `card-flush` with flat header (14px/600) matching the driver profile file pattern from Phase 23.
3. **Expiration rows**: Add accent border on table rows: `style="border-left:3px solid var(--red)"` for expired, `style="border-left:3px solid var(--amber)"` for warning.

**renderComplianceTruckFiles** (around line 27363-27392):
Same treatment as driver files above. Apply `data-table`, `card-flush`, accent-border + badge pattern.

**renderComplianceCompanyFiles** (around line 27407-27474):
1. Same table/card treatment as above.
2. **Folder items**: Replace `var(--bg-darker)` backgrounds with `var(--bg-secondary)`.

**renderComplianceDriverSection** (around line 27643+):
1. Tickets/violations section -- apply `data-table` to tables.
2. Replace any hardcoded colors with CSS variables.
3. Apply `badge-*` classes to status indicators.
4. If there's sub-navigation, check if it needs segmented-control treatment.

**Across ALL sub-functions:**
- Font weights capped at 600
- Zero hardcoded hex colors
- All tables have `data-table` class
- Expiration warnings use accent-border + badge pattern per CONTEXT.md

Commit the entire compliance page: `feat(25-03): restyle compliance page`

Do NOT change any file expiration calculation logic, upload handlers, or data fetching.
  </action>
  <verify>Search all renderCompliance* functions for: bg-darker, font-weight:700, font-weight:800. All should return zero. Count data-table occurrences in compliance functions -- should match the number of tables. Verify badge-red and badge-amber are used for expiration indicators.</verify>
  <done>All compliance sub-tabs use data-table, card-flush + flat header pattern for file sections, accent-border + badge pattern for expiration warnings. Company files use bg-secondary. Zero hardcoded hex, zero font-weight above 600 across all compliance functions.</done>
</task>

</tasks>

<verification>
1. Open the app, navigate to Compliance -- 7 tabs should display as a scrollable segmented control
2. Dashboard tab should show flat stat cards in a grid (no blue hero), section headers should be 14px/600
3. Click through each tab (Tasks, Driver Files, Truck Files, Company Files) -- all tables should have alternating row tint
4. Check expiration indicators -- expired items should have red left border + red badge, warning items should have amber left border + amber badge
5. Company Files folders should have bg-secondary backgrounds (not bg-darker)
</verification>

<success_criteria>
- Compliance tab bar is segmented-control-scroll (7 tabs)
- Dashboard hero replaced with flat stat-flat grid
- All compliance tables use data-table class
- Expiration warnings use accent-border + badge-red/badge-amber pattern
- File sections match Phase 23 driver profile pattern (card-flush + flat header + data-table)
- Zero hardcoded hex, zero gradients, zero font-weight above 600
- Single commit for entire compliance page
</success_criteria>

<output>
After completion, create `.planning/phases/25-operations-admin-restyle/25-03-SUMMARY.md`
</output>
