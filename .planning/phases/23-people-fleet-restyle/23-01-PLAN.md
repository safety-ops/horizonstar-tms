---
phase: 23-people-fleet-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Drivers page header shows 18px/600 text with no emoji icon"
    - "Add Owner-OP button is btn-secondary (not orange primary)"
    - "Driver cards have font-weight capped at 600 throughout"
    - "Driver profile detail view uses flat stat cards and CSS variable colors"
    - "Alert banner uses CSS variables instead of hardcoded hex"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderDrivers and viewDriverProfile"
      contains: "font-weight:600"
  key_links:
    - from: "renderDrivers"
      to: "base.css"
      via: "CSS component classes"
      pattern: "btn-secondary|badge-amber|stat-flat"
---

<objective>
Restyle the Drivers page (renderDrivers + viewDriverProfile) to the Stripe/Linear aesthetic.

Purpose: Drivers page is the first of 5 people/fleet pages to receive the v1.4 flat restyle treatment.
Output: renderDrivers and viewDriverProfile functions updated with flat styling, no hardcoded hex colors, font-weights capped at 600.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-people-fleet-restyle/23-CONTEXT.md
@.planning/phases/23-people-fleet-restyle/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle renderDrivers list view</name>
  <files>index.html</files>
  <action>
Find renderDrivers function (around line 22715-22779) and apply these changes:

1. **Page header**: Remove any emoji icon from the heading. Set heading to `font-size:18px;font-weight:600`. Change the "Add Owner-OP" button from `btn-primary` with `style="background:#f59e0b"` to `btn-secondary` (no inline background override). Keep "New Driver" as the only `btn-primary`.

2. **Alert banner** (document expiration alerts): Replace hardcoded `#fef2f2` background with `var(--bg-card)`. Replace `#fecaca` border with `var(--border)`. Add `border-left:3px solid var(--red)` for subtle accent. Replace any hardcoded `#dc2626` text color with `var(--red)`.

3. **Driver cards**: Find all `font-weight:700` and `font-weight:800` instances within the card template and cap at `font-weight:600`. This includes the driver name h4, trip count span, and earnings div.

4. **Owner-OP badge**: Replace any inline `background:#f59e0b` badge styling with the `badge-amber` CSS class from base.css.

5. **Other hardcoded colors**: Replace any remaining hardcoded hex colors (like `color:var(--success)` is OK but raw `#22c55e` is not) with CSS variable equivalents. The file count badges using `var(--dim-*)` are OK as-is.

Do NOT change the card grid layout structure (keep `repeat(auto-fill,minmax(280px,1fr))`). Do NOT change any click handlers or data logic.
  </action>
  <verify>Search renderDrivers for: font-weight:700, font-weight:800, #fef2f2, #fecaca, #f59e0b, #dc2626. All should return zero matches within the function. Confirm btn-primary appears only once (on New Driver button).</verify>
  <done>Drivers list page uses 18px/600 header, single btn-primary, flat alert banner with CSS variables, all font-weights at 600, Owner-OP badge uses badge-amber class.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle viewDriverProfile detail view</name>
  <files>index.html</files>
  <action>
Find viewDriverProfile function (around line 22781-23434, ~650 lines) and apply these changes:

1. **Profile header**: Cap any font-weight above 600 to 600. Replace any hardcoded hex colors with CSS variable equivalents.

2. **Stat cards**: Replace any colored background stat cards (e.g., `background:#dbeafe`, `background:#d1fae5`, `background:#fef3c7`, `background:#fae8ff`) with `.stat-flat` class pattern:
   - Use `class="stat-flat"` wrapper
   - Use `class="stat-flat-label"` for labels
   - Use `class="stat-flat-value"` with `style="font-family:var(--font-mono)"` for values
   - Remove all gradient or colored backgrounds

3. **Section headers**: Replace any emoji icons in section headings. Cap heading font-weights at 600. Use consistent `font-size:14px;font-weight:600` for section headings inside the profile.

4. **Tables within profile**: Add `class="data-table"` to any bare `<table>` elements. Ensure table wrappers use `.card-flush` if applicable.

5. **Status badges**: Replace any inline-styled status spans with `getBadge()` CSS classes where applicable.

6. **Hardcoded colors throughout**: Systematically replace hex colors:
   - `#16a34a` or `#22c55e` -> `var(--green)`
   - `#2563eb` or `#3b82f6` -> `var(--blue)`
   - `#f59e0b` or `#d97706` -> `var(--amber)`
   - `#dc2626` or `#ef4444` -> `var(--red)`
   - `#7c3aed` or `#8b5cf6` -> `var(--purple)`
   - `#64748b` or `#475569` -> `var(--text-secondary)`

7. **Button hierarchy**: Ensure only the primary action (if any) uses `btn-primary`. All other buttons should be `btn-secondary` or `btn-ghost`.

Do NOT change the profile layout structure or any data fetching/saving logic.
  </action>
  <verify>Search viewDriverProfile for: font-weight:700, font-weight:800, #dbeafe, #d1fae5, #fef3c7, #fae8ff, linear-gradient. All should return zero matches within the function. Grep for hardcoded hex patterns (#[0-9a-f]{6}) and verify only CSS variable references remain for colors.</verify>
  <done>Driver profile detail view uses flat stat cards, CSS variable colors throughout, font-weights capped at 600, consistent section headers, and proper button hierarchy.</done>
</task>

</tasks>

<verification>
1. Open the app and navigate to the Drivers page -- header should show "Drivers" at 18px/600 with no emoji, one green "New Driver" btn-primary, "Add Owner-OP" as btn-secondary
2. If there are document expiration alerts, they should show a flat card with subtle red left border accent
3. Driver cards should look clean with 600-weight text throughout
4. Click "View" on any driver -- profile page should show flat stat cards (no colored backgrounds), consistent typography, and CSS variable colors
</verification>

<success_criteria>
- renderDrivers has zero hardcoded hex colors, zero font-weight above 600, single btn-primary
- viewDriverProfile has zero gradient stat cards, zero hardcoded hex colors, zero font-weight above 600
- Both functions use established v1.4 CSS component classes (stat-flat, badge-*, data-table)
</success_criteria>

<output>
After completion, create `.planning/phases/23-people-fleet-restyle/23-01-SUMMARY.md`
</output>
