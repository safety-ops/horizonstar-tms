---
phase: 23-people-fleet-restyle
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Local Drivers page header shows 18px/600 text with no emoji icon"
    - "Local Drivers stat strip uses stat-flat class instead of colored stat-card backgrounds"
    - "Local Drivers table uses data-table class with card-flush wrapper"
    - "Pending Pickup and Delivery sections use neutral headers with subtle left border accent instead of colored card-headers"
    - "Trucks page header shows 18px/600 text with no emoji icon"
    - "Trucks table is clean with data-table class (already mostly done)"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderLocalDrivers, viewLocalDriverDetails, renderTrucks"
      contains: "stat-flat|data-table|card-flush"
  key_links:
    - from: "renderLocalDrivers"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|data-table|card-flush"
---

<objective>
Restyle Local Drivers page (renderLocalDrivers + viewLocalDriverDetails) and Trucks page (renderTrucks) to the Stripe/Linear aesthetic.

Purpose: Local Drivers has moderate complexity (stat strip + table + pending sections) and Trucks is minimal -- combining them keeps context budget efficient.
Output: Both pages updated with flat styling, CSS variable colors, proper component classes.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-people-fleet-restyle/23-CONTEXT.md
@.planning/phases/23-people-fleet-restyle/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle renderLocalDrivers and viewLocalDriverDetails</name>
  <files>index.html</files>
  <action>
Find renderLocalDrivers function (around line 23686-23854, ~170 lines) and apply these changes:

1. **Page header**: Remove any emoji icon (e.g., car icon). Set heading to `font-size:18px;font-weight:600`. Simplify the year filter -- remove any `surface-elevated` background wrapper, use just a `<select class="select">` with a plain label. Keep the primary add button as the only `btn-primary`.

2. **Stat strip**: Replace all `.stat-card` divs that have colored backgrounds (e.g., `background:#dbeafe`, `background:#d1fae5`, `background:#fef3c7`, `background:#fae8ff`, `background:#f1f5f9`) with `.stat-flat` class pattern:
   - `<div class="stat-flat">`
   - `<div class="stat-flat-label">Label</div>`
   - `<div class="stat-flat-value" style="font-family:var(--font-mono)">Value</div>`
   Remove all colored backgrounds. Keep the grid layout (`repeat(auto-fit,minmax(...))`).

3. **Driver table**: Add `class="data-table"` to the `<table>` element. Add `card-flush` class to the wrapping `.card` div. Ensure the card-header uses `padding:var(--space-3) var(--space-4)` and heading is `font-size:14px;font-weight:600;margin:0`.

4. **Pending Pickup section**: Replace the colored card-header (`background:#3b82f6;color:white`) with a neutral header: remove the background color, add `border-left:3px solid var(--blue)` to the card-header, set heading text to `color:var(--text-primary);font-size:14px;font-weight:600`. Replace the colored info bar below it (`background:#dbeafe`) with `background:var(--bg-secondary)` and text color `var(--text-secondary)`.

5. **Pending Delivery section**: Same treatment as Pending Pickup -- replace `background:#f59e0b;color:white` header with neutral + `border-left:3px solid var(--amber)`. Replace `background:#fffbeb` info bar with `background:var(--bg-secondary)`.

6. **Action buttons in pending sections**: Replace any hardcoded colored buttons with `btn-secondary` or `btn-ghost` classes. Only the main page CTA should be `btn-primary`.

7. **Font weights**: Cap all `font-weight:700` or `font-weight:800` to `font-weight:600` throughout the function.

8. **Hardcoded colors**: Replace all remaining hardcoded hex colors with CSS variable equivalents (same mapping as Plan 01 Task 2).

9. **viewLocalDriverDetails**: Search for this function (grep for `function viewLocalDriverDetails` or `viewLocalDriverDetails = function`). Apply the same treatment: cap font-weights at 600, replace hardcoded hex colors with CSS variables, use stat-flat for any colored stat cards, add data-table class to any tables.

Do NOT change the page layout structure, pending order card rendering (uses renderOrderPreviewCard which is already restyled), or any data logic.
  </action>
  <verify>Search renderLocalDrivers and viewLocalDriverDetails for: font-weight:700, font-weight:800, #dbeafe, #d1fae5, #fef3c7, #fffbeb, #3b82f6 (as background), #f59e0b (as background), surface-elevated. All should return zero matches within these functions. Confirm stat-flat class is used for stat cards. Confirm data-table class is on the driver table.</verify>
  <done>Local Drivers page uses flat stat cards, neutral section headers with left border accents, data-table class on tables, CSS variable colors, and font-weights capped at 600. Detail view is similarly restyled.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle renderTrucks</name>
  <files>index.html</files>
  <action>
Find renderTrucks function (around line 23435-23498, ~65 lines) and apply these changes:

1. **Page header**: Remove any emoji icon (e.g., truck icon). Set heading to `font-size:18px;font-weight:600`.

2. **Table verification**: The table already uses `.data-table` and `.card-flush` from prior work. Verify these classes are present. If any are missing, add them.

3. **Font weights**: Cap any `font-weight:700` or `font-weight:800` to `font-weight:600`.

4. **Hardcoded colors**: Replace any remaining hardcoded hex colors with CSS variable equivalents. Check both the desktop table and mobile card views.

5. **Mobile cards**: If there are mobile card views (`.mobile-only`), ensure they also use flat styling with no hardcoded hex colors.

6. **Status badges**: Ensure truck status badges use `getBadge()` CSS classes.

This is the simplest page -- likely only needs header cleanup and minor font-weight/color fixes.
  </action>
  <verify>Search renderTrucks for: font-weight:700, font-weight:800, and hardcoded hex patterns (#[0-9a-f]{3,8}). All should return zero matches or only CSS variable references. Confirm heading is 18px/600 with no emoji.</verify>
  <done>Trucks page uses 18px/600 header with no emoji, data-table class on table, no hardcoded hex colors, font-weights capped at 600.</done>
</task>

</tasks>

<verification>
1. Open the app and navigate to Local Drivers -- header should be clean 18px/600 text, stat strip should show flat neutral cards, table should have clean hairline styling
2. Pending Pickup section should have a neutral header with subtle blue left border (not full blue background)
3. Pending Delivery section should have a neutral header with subtle amber left border (not full amber background)
4. Click on a local driver to view details -- should show flat styling consistent with the list page
5. Navigate to Trucks -- header should be 18px/600 with no emoji, table should be clean with data-table styling
</verification>

<success_criteria>
- renderLocalDrivers uses stat-flat, data-table, card-flush classes with zero colored card-headers and zero hardcoded hex colors
- viewLocalDriverDetails uses flat styling with CSS variables
- renderTrucks has clean header and properly styled table with zero hardcoded hex colors
</success_criteria>

<output>
After completion, create `.planning/phases/23-people-fleet-restyle/23-02-SUMMARY.md`
</output>
