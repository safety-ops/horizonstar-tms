---
phase: 23-people-fleet-restyle
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Brokers page header shows 18px/600 text with no emoji icon"
    - "Broker summary cards use stat-flat class instead of gradient backgrounds"
    - "Broker card activity dots and score colors use CSS variables instead of hardcoded hex"
    - "Broker detail view uses flat stat cards and CSS variable colors"
    - "Dispatchers page header shows 18px/600 text"
    - "Dispatchers table uses data-table class"
    - "Dispatcher Ranking page uses flat stat cards, neutral spotlight cards, and data-table for ranking"
  artifacts:
    - path: "index.html"
      provides: "Restyled renderBrokers, viewBrokerDetails, renderDispatchers, renderDispatcherRanking"
      contains: "stat-flat|data-table|var\\(--green\\)|var\\(--blue\\)"
  key_links:
    - from: "renderBrokers"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|badge-|select"
    - from: "renderDispatcherRanking"
      to: "base.css"
      via: "CSS component classes"
      pattern: "stat-flat|data-table"
---

<objective>
Restyle Brokers page (renderBrokers + viewBrokerDetails) and Dispatchers pages (renderDispatchers + renderDispatcherRanking) to the Stripe/Linear aesthetic.

Purpose: Completes the people/fleet restyle. Brokers has the most work (gradient cards, hardcoded color helpers). Dispatchers + Ranking rounds out the phase.
Output: All 4 functions updated with flat styling, CSS variable colors, proper component classes.
</objective>

<execution_context>
@/Users/reepsy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/reepsy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-people-fleet-restyle/23-CONTEXT.md
@.planning/phases/23-people-fleet-restyle/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle renderBrokers and viewBrokerDetails</name>
  <files>index.html</files>
  <action>
Find renderBrokers function (around line 24399-24558, ~160 lines) and apply these changes:

1. **Page header**: Remove any emoji icon. Set heading to `font-size:18px;font-weight:600`. Replace inline-styled `<select>` elements with `class="select"`. Replace the colored period badge (`background:var(--dim-blue);color:var(--info);...border:1px solid var(--info-border)`) with neutral: `font-size:10px;background:var(--bg-tertiary);color:var(--text-secondary);padding:2px 6px;border-radius:4px`.

2. **Summary stat cards**: Replace the 4 gradient cards (`background:linear-gradient(135deg,#3b82f6,#1d4ed8)` etc.) with `.stat-flat` class pattern:
   ```
   <div class="stat-flat">
     <div class="stat-flat-label">Label</div>
     <div class="stat-flat-value" style="font-family:var(--font-mono)">Value</div>
   </div>
   ```
   Remove all `linear-gradient` backgrounds, `color:white` on these cards, and `font-weight:800`.

3. **Helper functions inside renderBrokers**:
   - `getScoreColor(score)`: Replace hardcoded hex returns (`#16a34a`, `#2563eb`, `#f59e0b`, `#dc2626`) with CSS variable references (`var(--green)`, `var(--blue)`, `var(--amber)`, `var(--red)`).
   - `getActivityStatus(broker)`: Replace hardcoded hex color dots (`#16a34a`, `#2563eb`, `#f59e0b`, `#dc2626`) with `var(--green)`, `var(--blue)`, `var(--amber)`, `var(--red)`.
   - `getScoreLabel(score)`: Review for any hardcoded colors (may just return text -- if so, leave as-is).

4. **Broker card grid**: Keep the grid layout. Within each card:
   - Cap all `font-weight:700` or `font-weight:800` to `font-weight:600`
   - Replace hardcoded medal/rank colors with CSS variables
   - Revenue/profit values: cap font-weight, ensure `font-family:var(--font-mono)` is present
   - Per-load metric pills using `var(--dim-*)` are OK as-is

5. **Hardcoded colors**: Systematically replace all remaining hex colors with CSS variables using the standard mapping.

6. **viewBrokerDetails** (around line 24618-24713, ~95 lines): Apply the same treatment:
   - Replace colored stat cards with `.stat-flat` class
   - Cap font-weights at 600
   - Replace hardcoded hex colors with CSS variables
   - Ensure any colored pills use CSS variable colors
   - Replace any colored info/broker cards with neutral flat styling
   - Add `.data-table` class to any tables

Do NOT change the broker card grid layout structure or any sorting/filtering logic.
  </action>
  <verify>Search renderBrokers and viewBrokerDetails for: linear-gradient, font-weight:700, font-weight:800, #3b82f6 (as background), #1d4ed8, #16a34a (in helper functions), #dc2626 (in helper functions). All should return zero matches. Confirm stat-flat class is used for summary cards. Confirm select class is on filter dropdowns.</verify>
  <done>Brokers page uses flat stat-flat summary cards (no gradients), CSS variable colors in all helpers, font-weights capped at 600, select class on filters, neutral period badge. Broker detail view uses flat styling throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle renderDispatchers and renderDispatcherRanking</name>
  <files>index.html</files>
  <action>
Find renderDispatchers function (around line 24715-24766, ~50 lines) and renderDispatcherRanking (around line 24835-25012, ~180 lines). Also check for any `rankedByCards` helper near line 25014.

**renderDispatchers:**

1. **Page header**: Set heading to `font-size:18px;font-weight:600`.
2. **Period filter**: Replace inline-styled `<select>` elements with `class="select"`. Replace any colored period badge with neutral: `font-size:10px;background:var(--bg-tertiary);color:var(--text-secondary);padding:2px 6px;border-radius:4px`.
3. **Table**: Add `class="data-table"` to the `<table>` element if missing. The wrapping `.card` should be fine as-is.
4. **Font weights**: Cap at 600.
5. **Hardcoded colors**: Replace with CSS variables.

**renderDispatcherRanking:**

1. **Page header**: Remove any trophy emoji icon. Set heading to `font-size:18px;font-weight:600`.
2. **Filter bar**: Replace inline-styled `<select>` elements with `class="select"`. Replace any colored period badge with neutral style.
3. **Stat strip**: Replace colored stat cards (colored backgrounds like `background:#dbeafe` etc.) with `.stat-flat` class pattern. Add `font-family:var(--font-mono)` on numeric values.
4. **Spotlight cards** ("TOP PERFORMER" / "NEEDS IMPROVEMENT"): Flatten these cards:
   - Remove large emoji/trophy icons
   - Replace colored/gradient backgrounds with `background:var(--bg-card);border:1px solid var(--border)` (flat card)
   - Add a subtle left border accent: `border-left:3px solid var(--green)` for top performer, `border-left:3px solid var(--amber)` for needs improvement
   - Cap font-weights at 600
   - Replace `color:white` text with `color:var(--text-primary)`
5. **Ranking table**: Add `class="data-table"` to the table. Remove any `rgba()` colored row backgrounds (like `background:rgba(34,197,94,0.1)` on top rows). If the top row needs subtle highlighting, use `background:var(--bg-secondary)` only.
6. **Progress bars**: Replace hardcoded `rgba()` colors in progress bar backgrounds and fills with CSS variables. Use `background:var(--bg-tertiary)` for track and `background:var(--green)` (or appropriate color) for fill.
7. **Leaderboard cards** (if separate from spotlight): Remove colored headers, use flat styling with neutral backgrounds.
8. **getMedal() / getProgressBar() helpers**: Replace any hardcoded hex or rgba colors with CSS variable equivalents.
9. **Font weights**: Cap all at 600 throughout both functions.
10. **Hardcoded colors**: Replace all remaining hex/rgba colors with CSS variables.

Do NOT change any ranking calculation logic, sorting, or data fetching.
  </action>
  <verify>Search renderDispatchers and renderDispatcherRanking for: font-weight:700, font-weight:800, linear-gradient, rgba( (in background contexts), #dbeafe, #d1fae5, trophy emoji. All should return zero matches. Confirm data-table class on both tables. Confirm stat-flat class on ranking stat cards. Confirm select class on filter dropdowns.</verify>
  <done>Dispatchers page uses data-table, clean header, and neutral period badge. Dispatcher Ranking uses flat stat cards, flattened spotlight cards with subtle border accents, clean data-table ranking, CSS variable colors in progress bars, and font-weights capped at 600.</done>
</task>

</tasks>

<verification>
1. Navigate to Brokers -- header should be 18px/600 with no emoji, summary cards should be flat (no gradient backgrounds), filter selects should use .select class
2. Click a broker to view details -- should show flat stat cards and CSS variable colors
3. Navigate to Dispatchers -- clean header, data-table styling on the table, neutral period badge
4. Navigate to Dispatcher Ranking -- flat stat cards, flattened spotlight cards (no large emoji, subtle border accents), clean ranking table with no colored row backgrounds
5. All pages should have consistent typography (600 max weight) and no hardcoded hex colors
</verification>

<success_criteria>
- renderBrokers has zero gradient stat cards, zero hardcoded hex in helpers, all font-weights at 600
- viewBrokerDetails uses flat styling with CSS variables
- renderDispatchers has data-table class and neutral styling
- renderDispatcherRanking has flat stat cards, flattened spotlights, clean ranking table, CSS variable progress bars
- All 4 functions use select class on filter dropdowns and neutral period badges
</success_criteria>

<output>
After completion, create `.planning/phases/23-people-fleet-restyle/23-03-SUMMARY.md`
</output>
